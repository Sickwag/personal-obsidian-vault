- 编程中使用 PreparedStatement 构造 sql 语句的意义是什么？
（为什么能防止 sql 注入？）
（为什么性能更好？）
- Agent 工具的工作原理是什么？
- Redis 持久化 RDB 和 AOF 区别？
- MySQL 索引为什么用 B+ 树？
- 聚簇索引和非聚簇索引区别？
- 回表是什么，怎么避免？
- 什么是最左前缀原则？
- 协程和线程的区别？什么情况下会选用协程？
- 协程能利用多核机器吗？


讲讲你对协程，线程，进程和线程的理解
A：线程是内核调度的执行单元，切换要陷入内核、上下文切换开销大（微秒级，涉及寄存器/栈/页表/缓存）；协程是用户态调度，切换只保存/恢复少量寄存器和栈指针（纳秒级），由程序控制。高并发网络场景线程数受限，用少量线程承载海量协程，提升吞吐。

协程和线程的数量级差距首先体现在内存占用上
A：线程默认栈约 8MB，内存受限，几万线程就扛不住；协程栈可指定小栈（几 KB 到几十 KB），几十万没问题。且协程切换不陷内核，吞吐更高。
其次体现在切换开销上

A：协程适合 **I/O 密集**（大量等 I/O）；**CPU 密集**或需要**强实时抢占**、依赖系统调度优先级时，线程/进程更合适。且协程若有阻塞调用会卡住整条线程（靠 Hook 解决）。

协程的状态机都有什么？
poll 的 ET 和 LT 区别？为什么用 ET？
A：LT 只要缓冲区有数据就持续通知（没读完会反复触发）；ET 仅状态**变化时通知一次**，必须一次读完。ET 减少系统调用与重复唤醒，配合非阻塞 I/O + 循环读写更高效，但要求读全、容易漏读，需在框架层保证一次读完。

高并发下 LT 每次有数据都唤醒，**重复系统调用和唤醒开销**大；ET 只在可读/可写状态变化时触发一次，配合循环读直到 EAGAIN，减少无谓唤醒，吞吐更高。

ET 容易踩什么坑？
1. 必须用**非阻塞** fd，否则读不全会阻塞
2. 必须**循环读到 EAGAIN**，否则漏数据（一次读不完整条消息）
3. 边沿触发要小心处理半包。框架层统一处理这些。

ZooKeeper 是什么？他和 Nacos 有什么区别？
ZooKeeper 临时节点和持久节点区别
so 动态库文件如何在程序运行时加载到程序中，让程序在不停机的情况下热更新？

增删改查之外，批量/分页怎么用同一套元数据？
JWT/Session/SessionId/Cookie 这些概念是什么？有什么区别？

什么是回表？性能问题在什么地方？如何解决？
什么是索引下推？
为什么要避免使用 `select *`？
什么是数据库的完整性？

