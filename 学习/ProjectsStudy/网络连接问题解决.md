## 使用 ssh 连接远程服务器
### 配置文件写法
配置文件在 windows 中一般为 `C:/User/username/.ssh/config`，linux 中一般为 `/etc/ssh/sshd_config` 或者 `~/.ssh/config `（Linux/macOS）`
SSH 配置文件是 OpenSSH 客户端的核心配置文件，允许用户为不同主机定制连接参数。该文件位于 `C:\Users\username\.ssh\config`（Windows）或，采用**分段式配置**结构，每段以 `Host` 开头定义一组规则。
#### 基础配置参数
##### 1. Host（必需）
- **作用**：定义配置块的名称/模式，支持通配符
- **用法示例**：
  ```config
  Host myserver  # 自定义别名
      HostName 192.168.1.100
  Host *.example.com  # 匹配所有example.com子域名
      User admin
  Host dev?  # 匹配dev1, dev2等
      Port 2222
  ```
##### 2. HostName
- **作用**：指定实际连接的主机名或 IP 地址
- **默认值**：`Host` 中指定的名称
- **示例**：
  ```config
  Host aws
      HostName ec2-12-345-678-90.us-west-2.compute.amazonaws.com
  ```
##### 3. User
- **作用**：指定登录用户名
- **默认值**：当前系统用户名
- **示例**：
  ```config
  Host github
      User git  # GitHub必须使用git用户
  ```
##### 4. Port
- **作用**：指定 SSH 服务端口
- **默认值**：22
- **应用场景**：
  ```config
  Host internal
      HostName 10.0.0.5
      Port 2222  # 非标准端口
  ```
##### 5. IdentityFile
- **作用**：指定私钥文件路径
- **默认值**：`~/.ssh/id_rsa`
- **高级用法**：
  ```config
  Host aws-prod
      IdentityFile ~/.ssh/aws_prod_key.pem
      IdentityFile ~/.ssh/backup_key  # 可指定多个密钥
  Host gitlab
      IdentityFile ~/.ssh/id_ed25519_gitlab  # 使用Ed25519算法
  ```

---
#### 安全与认证参数
##### 6. PreferredAuthentications
- **作用**：指定认证方法优先级
- **常用值**：`publickey,keyboard-interactive,password`
- **安全强化示例**：
  ```config
  Host secure-server
      PreferredAuthentications publickey  # 仅允许密钥认证
  ```
##### 7. IdentitiesOnly
- **作用**：是否仅使用配置中指定的密钥
- **典型场景**：
  ```config
  Host github
      IdentitiesOnly yes  # 避免使用ssh-agent中的其他密钥
  ```
##### 8. StrictHostKeyChecking
- **作用**：控制新主机密钥验证行为
- **可选值**：
  - `yes`（默认）：严格检查，拒绝未知主机
  - `no`：自动添加新主机密钥（不安全）
  - `ask`：询问用户（推荐）
  - `accept-new`：仅接受新主机（OpenSSH 7.6+）
- **示例**：
  ```config
  Host dev-servers
      StrictHostKeyChecking accept-new
  ```
##### 9. UserKnownHostsFile
- **作用**：指定 known_hosts 文件路径
- **应用场景**：
  ```config
  Host temp-server
      UserKnownHostsFile /dev/null  # 临时禁用主机验证
  ```

---
#### 连接优化参数
##### 10. ServerAliveInterval
- **作用**：发送保活包的时间间隔（秒）
- **默认值**：0（禁用）
- **推荐配置**：
  ```config
  Host *
      ServerAliveInterval 60  # 每60秒发送保活包
      ServerAliveCountMax 3   # 最大3次失败后断开
  ```
##### 11. ConnectTimeout
- **作用**：连接超时时间（秒）
- **应用场景**：
  ```config
  Host slow-network
      ConnectTimeout 10  # 10秒超时（默认为系统TCP超时）
  ```
##### 12. Compression
- **作用**：是否启用压缩
- **可选值**：`yes` / `no`
- **适用场景**：
  ```config
  Host high-latency
      Compression yes  # 高延迟网络下提升性能
  ```
##### 13. TCPKeepAlive
- **作用**：是否发送 TCP keepalive 消息
- **区别**：与 `ServerAliveInterval` 不同，这是 TCP 层的保活
- **配置**：
  ```config
  Host *
      TCPKeepAlive no  # 避免NAT超时断开（移动网络推荐）
  ```

---
#### 高级网络配置
##### 14. ProxyCommand
- **作用**：通过代理命令连接目标主机
- **常用场景**：
  ```config
  # 通过跳板机连接内网服务器
  Host internal
      ProxyCommand ssh -W %h:%p jump-host
  # 使用nc命令（netcat）
  Host legacy
      ProxyCommand nc %h %p 2> /dev/null
  ```
##### 15. ProxyJump
- **作用**：指定跳板机（OpenSSH 7.3+）
- **简化配置**：
  ```config
  Host db-server
      ProxyJump bastion  # 先连接bastion再跳转
  ```
##### 16. LocalForward / RemoteForward / DynamicForward
- **作用**：端口转发设置
- **示例**：
  ```config
  # 本地端口转发：将本地8080映射到远程80
  Host tunnel
      LocalForward 8080 localhost:80
  # 动态SOCKS代理
  Host proxy
      DynamicForward 1080
  ```

---
#### 实用配置模板
##### 1. GitHub 多账号配置
```config
# 主账号
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_main
    IdentitiesOnly yes
# 工作账号
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_rsa_work
    IdentitiesOnly yes
```
##### 2. AWS EC 2 连接优化
```config
Host aws-*
    User ec2-user
    IdentityFile ~/.ssh/aws_keys/%h.pem
    ServerAliveInterval 60
    StrictHostKeyChecking accept-new
Host aws-prod
    HostName 54.123.45.67
```
##### 3. 跳板机连接方案
```config
Host bastion
    HostName bastion.example.com
    User admin
    IdentityFile ~/.ssh/bastion_key
Host internal-server
    HostName 10.0.0.100
    ProxyJump bastion
    User deploy
```

---
### 配置验证与调试
#### 1. 语法检查
```bash
ssh -G example.com  # 输出解析后的配置
ssh -T -v example.com  # 详细连接调试
```
#### 2. 权限要求
- **Windows**：`.ssh` 目录需设为当前用户独占
- **Linux/macOS**：配置文件权限必须为 `600`
  ```bash
  chmod 600 ~/.ssh/config
  ```
#### 3. 高级技巧
- **条件配置**：使用 `Match` 指令（OpenSSH 7.5+）
  ```config
  Match User admin Exec "id -u > 500"
      IdentityFile ~/.ssh/special_key
  ```
- **环境变量**：使用 `%` 变量
  - `%h`：目标主机名
  - `%p`：目标端口
  - `%r`：用户名
  - `%d`：本地用户家目录

> 💡 **重要提示**：配置更改后**无需重启服务**，SSH 客户端每次连接时自动读取最新配置。多段配置按**从上到下匹配第一个符合的规则**，建议将通用配置放在文件末尾。
### finnalshell 连接方式
finnalshell 一直提示输入密码原因：
- 用户名输入有误
- 服务器中的 `/etc/ssh/sshd_config` 配置文件中没有开启密码登录
	- **允许 root 登录**（如果用 root 账号连接）  `PermitRootLogin yes`  注意解除注释
	- **允许密码登录**（默认可能被禁用）  	`PasswordAuthentication yes`

正常使用密码登录
![[Pasted image 20250814143852.png]]
如果使用私钥文件登录
![[Pasted image 20250814143958.png]]
如果私钥文件设置了密码输入即可，没有设置私钥密码可以不输入
### 代码连接方式
参考代码 [[C++ practice case#tcp 连接]]
编写代码通过 tcp 连接服务器服务器无响应原因：
连接之前需要注意：
- 服务器安全组放开 6379 端口
- redis. conf 文件中将 bind 设置为需要连接主机的 ip 地址，或者设置为 `0.0.0.0` 允许所有 ip 介入 redis 服务
- 使用 `ufw status | grep 6379` 检查防火墙是否允许 6379 端口流量进入，如果没有则使用
```bash
ufw allow 6379/tcp
systemctl restart ufw
```
放行并重启防火墙（ufw[^1]）
[^1]: `ufw`（Uncomplicated Firewall）是一个用户友好的前端界面，用于管理 `iptables` 防火墙规则。它是为了简化 `iptables` 过复杂的规则和命令而设计的。
### 使用密钥对连接
#### 传统密钥对连接
##### 创建密钥对
假设有两台主机A和B，A想要连接B，那么就需要在A本地使用
```bash
ssh-keygen -t rsa -b 4096
```
创建一个以rsa加密的密钥对，其中：
- **`-t rsa`**：指定密钥类型为 **RSA**（还可以选 `ed25519`、`ecdsa` 等）。
- **`-b 4096`**：指定密钥长度为 **4096 位**（越长越安全，但计算稍慢）。
- 生成的文件：
    - **`~/.ssh/id_rsa`**（私钥，必须严格保密，权限 `600` 保证可读可写）。
    - **`~/.ssh/id_rsa.pub`**（公钥，可以自由分发）。
将公钥上传到服务器 B 中
```bash
ssh-copy-id user_name@B_address
# 如果不支持ssh-copy（常见于windows电脑中），可以使用
cat ~/.ssh/公钥名称.pub | ssh user_name@B_address "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"
# 本质上时先连接上远程服务器，用命令创建文件夹后将**本地公钥文件内容**cat写入服务器中的authorized_keys文件中。
```
authorized_keys 这个文件用来存储**所有的公钥**，每行一个，每次新建时使用 cat 填充到末尾即可
##### 密钥对识别原理
- 当 `A` 连接 `B` 时：
    1. `A` 发送自己的公钥 `id_rsa.pub` 给 `B`。
    2. `B` 检查 `~/.ssh/authorized_keys` 是否有匹配的公钥。
    3. 如果找到匹配的公钥，`B` 生成一个随机数，用 `A` 的公钥加密，并发送给 `A`。
    4. `A` 用自己的私钥解密，并返回解密结果。
    5. `B` 验证解密是否正确，如果正确则允许登录。
- B 想要连接 A 则反之 A 必须要存储 B 中生成的公钥
当多台主机的公钥都通过 cat 加入 authorized_keys 文件中，每次连接时，系统会使用请求连接端的私钥**逐一匹配**存储的公钥。
- 如果 `A` 的公钥匹配其中一行，则继续挑战-响应验证。
- 如果没有任何一行匹配，则拒绝连接
- **SSH 服务器不需要知道哪个公钥对应哪台机器**，只关心客户端能否提供匹配的私钥
windows 中，`C:/User/username/.ssh` 中的 config 文件存储了连接配置
```config
Host serverA
    HostName 192.168.1.100
    User alice
    IdentityFile ~/.ssh/id_rsa_alice  # 指定不同私钥
Host serverB
    HostName 192.168.1.101
    User bob
    IdentityFile ~/.ssh/id_rsa_bob
```
如果配置得当，使用 `ssh username@ip_address` 链接时，通过读取配置即实现**免密来接**
#### 阿里云服务器连接
阿里云ECS服务器上获得的 `.pem` 文件是 **SSH 私钥文件**，用于免密码登录你的云服务器，阿里云用它替代传统的密码登录，提供更高的安全性（密钥比密码更难破解）。
阿里云ECS的默认登录方式
1. **不依赖 `authorized_keys`**：
    - 传统SSH免密登录需要将公钥写入 `~/.ssh/authorized_keys`，但阿里云使用了 **实例初始化注入** 的方式。
    - 云服务器在首次启动时，阿里云会将你的**公钥直接注入系统**（可能通过 `cloud-init` 或阿里云内部机制），但不会在用户目录生成 `authorized_keys` 文件。
2. **直接使用 `.pem` 私钥连接**：
    - 你无需手动配置公钥，只需在SSH命令中指定私钥文件即可登录。
## 端口号，进程号之间查询
总结

| 操作 | 命令 | 优势 | 适用场景 |
|------|------|------|----------|
| **查端口→进程** | `sudo lsof -i :8888` | 显示完整进程路径 | 需要精确进程路径时 |
|  | `sudo ss -ltnp 'sport = :8888'` | 性能最优 | 高并发服务器 |
|  | `sudo netstat -tulnp \| grep :8888` | 兼容老系统 | CentOS 6 等旧系统 |
| **查进程→端口** | `sudo lsof -p 1839 -a -i` | 信息最全 | 深度调试 |
|  | `ls -l /proc/1839/fd \| grep socket` | 无需额外工具 | 极简环境 |
| **终止进程** | `sudo fuser -k 8888/tcp` | 精准端口匹配 | 安全终止 |
|  | `kill -9 $(lsof -t -i :8888)` | 一行命令 | 快速解决 |
### 端口号查询进程信息
#### lsof（需要安装 lsof）
```bash
# 查询指定端口的进程（如8888）
sudo lsof -i :8888
# 示例输出解读：
# COMMAND   PID   USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
# python3  1839   root    3u  IPv4  24567      0t0  TCP *:8888 (LISTEN)
#  ▲        ▲      ▲     ▲    ▲
#  进程名   PID   用户  文件描述符 协议
lsof -i :8888
COMMAND     PID USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
BT-Panel 103911 root    5u  IPv4 860643      0t0  TCP *:8888 (LISTEN)
```
宝塔面板占用 8888 端口
- `-i`：列出网络连接
- `-P`：禁止端口号转换为服务名
- `-n`：禁止IP地址解析为主机名
进阶命令：
```bash
# 同时查看TCP/UDP所有端口
sudo lsof -i -P -n | grep LISTEN
# 查看特定用户的端口
sudo lsof -i :8888 -u your_username
# 显示完整进程路径
sudo lsof -p $(lsof -t -i :8888) -a -d txt
```
#### netstat（不同 linux 系统兼容）
```bash
# 查询端口占用（需sudo才能看到PID）
sudo netstat -tulnp | grep :8888
# 参数详解：
# -t = TCP, -u = UDP, -l = 仅监听端口
# -n = 禁用DNS解析, -p = 显示PID/进程名
netstat -tulnp | grep 8888
tcp        0      0 0.0.0.0:8888            0.0.0.0:*               LISTEN      103911/python3  
```
会显示运行程序的**可执行文件名称**，BT-panel 是 python 程序，所以获取到的是 python 3 而不是进程自己设置的名称（ss 获取这个名称）
#### ss (性能最优，现代 os 用来替代 netstat)
```bash
# 查询端口的进程
sudo ss -ltnp 'sport = :8888'
# 参数说明：
# -l = 监听状态, -t = TCP, -n = 端口号显示数字
# -p = 显示进程信息, 'sport = :端口' = 源端口过滤
ss -ltnp 'sport = :8888'
State    Recv-Q   Send-Q     Local Address:Port     Peer Address:Port  Process   
LISTEN   0        128              0.0.0.0:8888          0.0.0.0:*      users:(("BT-Panel",pid=103911,fd=5))
```
### PID查询进程占用的端口
#### lsfo 查看进程所有网络连接
```bash
# 查询PID 1839的所有网络连接
sudo lsof -p 1839 -a -i
# -a = 逻辑与, -i = 网络连接过滤
lsof -p 103911 -a -i
COMMAND     PID USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
BT-Panel 103911 root    5u  IPv4 860643      0t0  TCP *:8888 (LISTEN)
```
#### netstat 匹配进程号
```bash
# 通过PID查询端口
sudo netstat -tulnp | grep 1839
# 或精确匹配（避免PID子串匹配）
sudo netstat -tulnp | awk -v pid="1839" '$0 ~ " "pid"/" {print}'
```
**参数说明**：

- `-t`：显示TCP端口
- `-u`：显示UDP端口
- `-l`：仅显示监听状态的端口
- `-n`：以数字形式显示端口（不解析服务名称）
- `-p`：显示占用端口的进程PID和名称
## redis 无法远程连接
### 配置问题
首先使用 `sudo systemctl cat redis` 查看 redis 启动脚本中的设置，关注ExecStart 参数，可能会看到
```sh
ExecStart=/usr/bin/redis-server /etc/redis.conf --supervised systemd
```
其中第二个参数表示配置文件位置
检查 `/etc/redis/redis.conf` 或者 `/etc/redis.conf` 文件中
```conf
bind 0.0.0.0
```
正常第一次安装默认127.0.0.1，这样只会监听本地 6379 端口
```conf
port 6379
```
端口设置，决定在什么端口上监听。port 和 bind 两个参数在配置文件中可能有多行，只修改一处可能会导致配置被覆盖，可以使用 `sudo grep -n "^bind" /etc/redis.conf` 查看所有 bind 行，其他配置同理
```confi
daemonize no
```
- `daemonize yes`  
    Redis 启动后会**自己 fork 到后台**，shell 立即返回，进程 ID 写入 `pidfile` 指定的文件。这是 redis 只会在前台运行，`ctrl+C` 退出到命令行之后 redis 停止
- `daemonize no`  
    Redis **保持在前台运行**，日志直接输出到标准输出/标准错误；**systemd 建议设为 no**，因为 systemd 自己会负责把进程放到后台并管理日志，再 daemonize 反而会导致双重 fork，日志难以收集。
这样服务器端启动之后，可以再客户端使用连接
```bash
redis-cli -h <address> -p <port> -a <password>
```
## 宝塔面板管理
### node 项目网站无法启动&访问
无法启动可能是：
1. 没有安装较新版本的 node，在项目管理页面中，node 版本中显示所有版本安装一个较新的 LTS 即可
   ![[Pasted image 20250818153643.png]]
2. 没有安装项目的模块
   ![[Pasted image 20250818153723.png]]
3. 项目配置问题
   ![[Pasted image 20250818153747.png]]
   默认项目目录是 `www/wwwroot`，运行用户为 www，如果修改项目目录为其他，需要**注意运行用户是否有权限**访问运行目录中的文件，如果运行用户是 www，而项目目录在其他用户的文件夹中，就可能会出现错误无法启动。
```error
启动失败npm error code ENOENT npm error syscall open npm error path /www/server/panel/package.json npm error errno -2 npm error enoent Could not read package.json: Error: ENOENT: no such file or directory, open '/www/server/panel/package.json' npm error enoent This is related to npm not being able to find a file. npm error enoent npm error Log files were not written due to an error writing to the directory: /www/server/nodejs/cache/_logs npm error You can rerun the command with `--loglevel=verbose` to see the logs in your terminal
```
