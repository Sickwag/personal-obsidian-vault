## 浮点数精度丢失问题
参考专栏：[浮点数的底层原理和精度损失问题 - 知乎 (zhihu.com)](https://zhuanlan.zhihu.com/p/269619376)
参考视频：[为什么浮点数运算不够精确_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV14w4m1k71N/?spm_id_from=333.337.search-card.all.click&vd_source=876be08bc9c030f4a9ea1fb97e0d0342)
### 小数的[二进制](https://zhida.zhihu.com/search?q=%E4%BA%8C%E8%BF%9B%E5%88%B6&zhida_source=entity&is_preview=1)表示

浮点数的表示借鉴了[科学计数法](https://zhida.zhihu.com/search?q=%E7%A7%91%E5%AD%A6%E8%AE%A1%E6%95%B0%E6%B3%95&zhida_source=entity&is_preview=1)，比如在十进制中 311.56 可以表示成 3.1156∗10^2 。类似地，浮点型数据的二进制存储结构也可以被划分成：**符号位 s + 指数位 e + 尾数位（有效数字位）m**。按照国际标准 IEEE 754，任意一个二进制浮点数可以表示成： 𝑉=(−1)^𝑠×𝑀×2^𝐸   其中：

- (−1)^𝑠 表示符号位， 𝑠=0 表示正数， 𝑠=1 表示负数
- 𝑀 表示有效数字， 1≤𝑀<2
- 2^𝐸 表示指数位

> 指数部分决定了数的大小范围，有效数字部分决定了数的精度。

在位运算中，n 进制数\*进制基数（即 n） 表示将所有数字向右移动一位，如 1010 二进制数\*得 10100

|十进制|二进制|二进制科学计数法|S|E|M|
|---|---|---|---|---|---|
|3.0|11.0|1.1 x 2^1|0|1|1.1|
|-5.0|-101.0|-1.01 x 2^2|1|2|1.01|

### 浮点数在计算机底层的存储机制

> double 类型和 float 类型（可能还有 long double 类型）在计算机的底层存储结构都是一致的，唯一的不同在于 float 是 32 位而 double 是 64 位的。

32 位浮点数在内存中划分机制
![Pasted image 20240826195547.png](../../Files%20&%20LongText/Attachments/Pasted%20image%2020240826195547.png)
计算机中小数的表示按照小数点的位置是否固定可以分为浮点数和定点数。为了方便和 float 32 浮点数做对比，我们构造一个 32 位精度的定点数，其中小数点固定在 23 bit 处：
![v2-cb2ae52724d3d8c4b2d52934ca495f18\_720w.webp](../../Files%20&%20LongText/Attachments/v2-cb2ae52724d3d8c4b2d52934ca495f18_720w.webp)

其中 IEEE 754 的规定为：

- 因 M 的取值一定是 1<=M<2，因此规定 M 在存储时舍弃第一个 1，只存储小数点后的数字，这样可以节省存储空间（以 float 32 为例，可以保存 23 位小数信息）
- 指数 E 是一个无符号整数，因此它的取值范围为 0255，但指数可以是负的，所以规定在存入 E 时在它原本的值加上 127（使用时减去中间数 127），这样 E 的取值范围为-127128
- E 不全为 0，不全为 1：正常的计算规则，E 的真实值就是 E 的字面值减去 127（中间值），M 加回 1
- E 全为 0：指数 E 等于 1-127 为真实值，M 不加回 1（此时 M 为 0. xxxxxx），这样是为了表示 0 和一些很小的数
- E 全为 1：M 全为 0 表示正负无穷大（取决于 S 符号位）；M 不全为 0 时表示不是一个数（NaN）
![Pasted image 20240826200022.png](../../Files%20&%20LongText/Attachments/Pasted%20image%2020240826200022.png)
`𝑉=(−1)^𝑠×𝑀×2^𝐸`  e 为 0 表示 e 的 0-127=-127 次方乘以一个 1<=M<=2 的数值，得到一个非常小的数。正负无穷大

### 转换的具体例子
#### 无精度损失
10.25 转换二进制存储在计算机中过程：
10.25 转换为 2 进制为 1010.01
![400](../../Files%20&%20LongText/Attachments/Pasted%20image%2020240826194617.png)
使用 IEEE 标准公式转换为 1.01001\*2^3：
10.25 为正数，所以 s = 0，小数位数 5 位，所以 M = 01001
10.25 是正数\*自然对数的正次幂，所以 E 的字面值为 127+3 = 130
将三个数填充如计算机存储空间中：
![一般的编程语言给float赋4字节32位空间](../../Files%20&%20LongText/Attachments/Pasted%20image%2020240826195250.png)
使用指针查看数据在内存中地址就是 0 x 4121000

#### 有精度损失
十进制中 0.5（代表分数 1/2）表示二进制中 0.1（等同于分数 1/2），我们可以把十进制中小数部分乘以 2 作为二进制的一位，然后继续取小数部分乘以 2 作为下一位，直到不存在小数为止。

以 0.2 这个无法精确表示成二进制的浮点数为例：

0.2×2=0.4→
00.4×2=0.8→
00.8×2=1.6→
10.6×2=1.2→
10.2×2=0.4→
0...... (0.2) 10=(0.00110...) 2 
因此十进制下的 0.2 无法被精确表示成二进制小数，这也是为什么十进制小数转换成二进制小数时会出现精度损失的情况。

### 提高精度的方法
使用更高精度的数据类型，如 double 等，操作系统会为其分配更大的存储空间
- [单精度浮点数](https://zhida.zhihu.com/search?q=%E5%8D%95%E7%B2%BE%E5%BA%A6%E6%B5%AE%E7%82%B9%E6%95%B0&zhida_source=entity&is_preview=1) float 占四个字节，表示范围-3.40 E+38 ~ +3.40 E+38
- [双精度浮点数](https://zhida.zhihu.com/search?q=%E5%8F%8C%E7%B2%BE%E5%BA%A6%E6%B5%AE%E7%82%B9%E6%95%B0&zhida_source=entity&is_preview=1) double 占八个字节，表示范围-1.7 E+308 ~ +1.79 E+308
## http和https协议区别
### 简要说明
我们可以用一个非常形象的比喻来理解：
*   **HTTP** 就像**寄送明信片**。你写在明信片上的所有内容（账号、密码、聊天记录、信用卡号）在传递过程中，每个经手的人（网络管理员、路由器、甚至黑客）都能看得一清二楚。
*   **HTTPS** 就像**用一把只有你和收件人才有的**专属钥匙**，把信件锁在**安全的保险箱**里寄送。即使中途被人截获，他们也无法打开保险箱看到里面的内容。
下面我们从技术层面详细拆解它们的区别：
### 核心区别对比表

| 特性           | HTTP (超文本传输协议)                            | HTTPS (超文本传输安全协议)                   |
| :----------- | :---------------------------------------- | :---------------------------------- |
| **安全性**      | **不安全**                                   | **安全**                              |
| **协议**       | 应用层协议                                     | HTTP + SSL/TLS 协议                   |
| **端口**       | 80                                        | 443                                 |
| **加密**       | **无加密**，数据以明文传输                           | **有加密**，数据传输是加密的                    |
| **证书**       | 不需要数字证书                                   | 需要由受信任的**数字证书认证机构 (CA)** 颁发的 SSL 证书 |
| **身份认证**     | 无法验证网站身份，容易遭遇**钓鱼网站**                     | 验证网站所有者的身份，确保你访问的是真实网站              |
| **数据完整性**    | 数据在传输过程中容易被篡改，无法察觉                        | 数据带有消息摘要，一旦被篡改，连接会中断                |
| **SEO & 性能** | 现代浏览器（如 Chrome）会标记为“不安全”，对搜索引擎排名 (SEO) 不利 | 浏览器显示安全锁图标，是 Google 等搜索引擎的排名加分项     |
| **应用场景**     | 不涉及敏感信息的网站，如资讯类、博客                        | 所有涉及隐私和交易的网站，如**电商、网银、邮箱、登录页面**     |

---

### 详细解释三个核心不同点

#### 1. SSL/TLS 加密 (安全性)
这是 HTTPS 的基石。
*   **HTTP**：数据在网络中以明文形式传播，就像上面说的“明信片”。
*   **HTTPS**：在 HTTP 和 TCP 层之间加入了一个**SSL/TLS 加密层**。所有在浏览器和服务器之间传输的数据都会先经过加密，变成一堆乱码，即使被黑客截获，他也无法解密出原始内容。

#### 2. 身份认证 (可信性)
这解决了“你访问的网站是不是真的”这个问题。
*   **HTTP**：你无法确认你正在访问的“www.abc-bank.com”是不是黑客伪造的假网站。
*   **HTTPS**：网站需要向**证书颁发机构 (CA)** 申请一个**SSL 证书**。这个证书就像网站的“身份证”，证明了该网站的真实身份。当你访问时，浏览器会检查这个证书是否有效、是否由可信机构颁发。如果证书有问题，浏览器会发出明确的警告。

#### 3. 数据完整性 (防篡改)
这确保了数据在传输过程中没有被修改。
*   **HTTP**：数据在传输途中可能被中间人拦截并篡改（例如，插入广告或恶意代码），而你和服务器都无从得知。
*   **HTTPS**：利用加密机制，它会为传输的数据生成一个唯一的“指纹”（摘要）。如果数据在传输过程中被改动了一丁点，这个指纹就会对不上，连接就会立即终止，从而防止数据被篡改。

### 为什么现在所有网站都应该使用 HTTPS？

1.  **保护用户隐私**：这是最重要的原因。防止用户的密码、身份证号、聊天记录、搜索历史等敏感信息被窃取。
2.  **建立信任**：浏览器地址栏的**小锁图标** 会给用户安全感，让他们更愿意在你的网站上进行操作和交易。
3.  **SEO 优势**：Google 等主流搜索引擎明确表示，使用 HTTPS 是搜索排名的正面因素。
4.  **浏览器推动**：现代浏览器（如 Chrome、Firefox）会将仍使用 HTTP 的网站标记为“**不安全**”，这会极大地吓跑用户。
5.  **适配现代技术**：许多新的 Web 平台特性（如地理位置、Service Worker 等）都要求网站必须使用 HTTPS。

### 总结

简单来说，**HTTPS 就是 HTTP 的安全升级版**。它通过**加密、身份认证和数据完整性保护**这三项技术，为我们在互联网上的通信提供了基本的安全保障。在今天，HTTPS 已经不再是“可选项”，而是网站运营的“标配”。任何时候，当你需要输入任何个人信息时，都请务必确认浏览器地址栏显示的是 **`https://`** 并且带有一把**锁形图标**。

## 什么是环境变量
参考教程：[『教程』什么是环境变量_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1w741147G9/?spm_id_from=333.337.search-card.all.click&vd_source=876be08bc9c030f4a9ea1fb97e0d0342)
**定义：** 环境变量是在操作系统中一个具有特定名字的对象，它包合了一个或者多个应用程序所将使用到的信息。

环境变量的主要作用只有：*指明操作系统的重要目录在哪里，使用运行窗口时运行的程序名会在环境变量的 Path 路径中查找并运行*

用户变量和系统变量主要区别在作用范围

### 事实
- SystemRoot 是 windows 中一个特殊环境变量名，cmd 中运行 `%SystemRoot%` 会打开系统目录（一般在 C 盘的 Windows 文件夹中）
- 输入 cmd 打开命令提示符

### 为什么？
运行窗口工作原理
![Pasted image 20240829210656.png](Pasted%20image%2020240829210656.png)
cmd 应用程序在 `C:\SystemRoot\system 32\cmd. exe` 位置
查看 Path 路径
![Pasted image 20240829210808.png](Pasted%20image%2020240829210808.png)

### 自定义运行
1. 将自定义运行程序放在环境变量指定路径中
2. 创建文件夹专门放需要在运行窗口中输入运行，将文件夹路径放入环境变量

## 什么是结构体内存对齐
参考视频：[到底为什么要内存对齐？哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1aV4y1y7Sd/?spm_id_from=333.337.search-card.all.click&vd_source=876be08bc9c030f4a9ea1fb97e0d0342)
### 理论和实际的冲突
C/C++中，结构体中默认所有数据放在一整块连续的内存中，理论和实际有冲突
![Pasted image 20241021210727.png](Pasted%20image%2020241021210727.png)
通过互查看汇编源码可以得到其内存布局
![Pasted image 20241021210839.png](Pasted%20image%2020241021210839.png)
**对齐规则**
1、第一个成员在与结构体偏移量为 0 的地址处。
2、其他成员变量要对齐到某个数字（对齐数）的整数倍的地址处
	对齐数=编译器默认的对齐数与该成员大小的**较小值**。
3、结构体总大小为最大对齐数（每个成员都有一个对齐数）的整数倍。

### 为什么需要内存对齐
- 寄存器只能从内存地址是 4 的倍数的位置开始读取数据（与处理器架构和位数也有关系）
![Pasted image 20241021211403.png](Pasted%20image%2020241021211403.png)
- 对齐的对齐数可以手动指定
- 从硬件层面分析
- ![Pasted image 20241021212144.png](Pasted%20image%2020241021212144.png)
一个主板上插入的内存条中有多个 chip，这些 chip 共同组成一个连续的存储空间

## 屏幕密度和像素概念区分
基本概念铺垫
### 像素
像素 (pixel) 为图像的基本单位，是一个抽象的采样，在很多情况下，它们采用点或者方块显示。每个像素可有各自的颜色值，可以采用 RGB 表示。单位面积内的像素越多代表分辨率越高，所显示的图像就会接近于真实物体。
### 屏幕尺寸
屏幕尺寸 (Screen size) 指屏幕的物理尺寸，一般用屏幕对角线长度表示，单位英寸 (1 英寸 = 2.54 厘米)。如小米 4 是 5 英寸。
### 宽高比率
宽高比率 (Aspect Ratio) 指的是实际屏幕的物理尺寸宽高比，如 16：9，4：3 等等。
### 屏幕密度
屏幕密度 (Screen density) 指的是屏幕里像素值浓度，分辨率/屏幕尺寸可以反映出手机密度。为了方便，Android 将屏幕密度分为 6 种，low, medium, high, extra-high, extra-extra-high, and extra-extra-extra-high.
### 分辨率
分辨率 (Resolution) 和电脑的分辨率概念一样，指手机屏幕纵、横方向像素个数。如 1920*1080，它表示纵向一列有 1920 个像素，横向一行有 1080 个像素。
### DPI
DPI (dot per inch)，每英寸像素数，可以反映屏幕的清晰度，可用于缩放 UI。
例如，如果一个 320×240 分辨率的屏幕之物理尺寸为 2 inch×1.5 inch，
那么 DPI = 320/2 = 240/1.5 =160。一般的，计算的方法为对角线像素值/对角线长度 (屏幕尺寸)。如计算 WVGA (800*480) 分辨率，3.7 英寸的密度 DPI 为 sqrt (800^2 + 480^2)÷3.7 = 252。密度（字面意思）
### dip (dp)
DIP = Density-independent pixel，与密度无关像素，亦称为设备独立像素 (Device independent pixel)，是一个虚拟的像素的单元，其等价于 160 dpi 屏幕的一个实际物理像素。这种 160 dpi 的屏幕的屏幕密度是系统假设一个基准密度 (也就是 medium 级别的密度)，在运行时，系统会将 DP 值根据不同 DPI 的屏幕转化具体的物理的像素。公式为 px = dp * (dpi / 160)。如一个 240 dpi 的屏幕，1 dp 在其上实际等于 1.5 个物理像素 px。在布局时应该使用 dp 作为单位以方便适配
### Android 对手机屏幕尺寸和 dpi 分级的情况
具体的数据 (适配常用，重点关注)
ldpi (low) ~120 dpi
mdpi (medium) ~160 dpi
hdpi (high) ~240 dpi
xhdpi (extra-high) ~320 dpi
xxhdpi (extra-extra-high) ~480 dpi
xxxhdpi (extra-extra-extra-high) ~640 dpi

## ascii Unicode utf-8编码
参考视频教程：[ASCII、Unicode和UTF-8编码详解，一次彻底弄明白！简单易懂_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV14Ns8eEERu/?spm_id_from=333.1007.tianma.1-1-1.click&vd_source=876be08bc9c030f4a9ea1fb97e0d0342)
最原始的字符和数字进行映射的方案是 ascii
![Pasted image 20240929174428.png](Pasted%20image%2020240929174428.png)
ascii 真正编码位只有 7 个，最前一位固定是 0

![Pasted image 20240929180207.png](Pasted%20image%2020240929180207.png)
所有的支持的字符都有一个唯一的编码
最小的，有意义的书写符号到位叫做**字素**
![Pasted image 20240929181350.png](Pasted%20image%2020240929181350.png)
编码规则用来解决二进制编码读取过程中混乱问题
![同一个二进制编码，可以解析出不同的结果](Pasted%20image%2020240929181513.png)
- UTF-32 和 ascii 一样是定长编码，无论什么字符都会占用 4 个字节（而不是 ascii 的 1 个），空间浪费较为严重
- utf-8 是一种**变长**（意为改变长度）的编码，不同的字符规定不同的字节大小（英文仍用 1 个字节，完全适配 ascii，中文三个，其他较为复杂的最多 4 个）
![Pasted image 20240929181940.png](Pasted%20image%2020240929181940.png)
![Pasted image 20240929182223.png](Pasted%20image%2020240929182223.png)
将一个  Unicode 编码转换为 utf-8 编码方式是将 Unicode 代码点转换为二进制，然后根据 utf-8 的编码固定格式**从后向前**填充，未满的位置用 0 填充，解码则逆向即可
![recording 18.gif](recording%2018.gif)
1. 数据的解码和编码，需要使用同一种编码 (GBK、UTF-8)
否则会发生乱码
2. Unicode 编码中：1 字素 1 代码点 1 字节
- 字符串只包含 ASCII 编码的字符，可以直接处理
- 字符串包含中文、表情包等，需要按照 Unicode 字符串来处理，不同编程语言可能不同（Python、Golang）
- 如果想要更准确的处理字符串，比如字符串中
包含这种字符：![75](Pasted%20image%2020240929183111.png)，需要使用 grapheme 函数

## Base 64 编码规则
- 常见问题

- base 64 编码相较于 ansc 编码有什么优点？
- 在传输中使用到 base 64 编码相较于 ANSC 码还需要多转码，解码，编码的步骤。为什么不能直接使用兼容性更好，内容也更多的 ANSC 码？
- ansc 码转 base 64 过程中末尾补齐的 0 为什么不会影响到解码后的内容？
- 如何知道 ansc 码转 base 64 过程中末尾补齐了多少个 0？
- 如果我想进一步了解 base 64 编码的原理和更多知识，我应该去看那些文章或者书籍？

解答

[Base64编码相较于ANSI编码的优点主要体现在以下几个方面](https://www.zhihu.com/question/36306744) [1](https://www.zhihu.com/question/36306744) [2](https://zhuanlan.zhihu.com/p/384238870) [3](https://zhuanlan.zhihu.com/p/371596658)：

1. **兼容性**：Base 64 编码后的字符串只包含 ASCII 基础字符，这些字符在大多数机器和软件上的行为是一样的，因此具有良好的兼容性。
2. [**避免特殊字符处理**](https://www.zhihu.com/question/36306744) [：某些协议会对二进制流中特定字符进行特殊处理（比如ASCII的0~32编码的字符在某些传输介质中，是会被当作特殊含义处理的），这种时候就需要通过编码来避开这些特定字符](https://www.zhihu.com/question/36306744) [1](https://www.zhihu.com/question/36306744)。

[在传输中使用Base64编码相较于ANSI码的原因主要是因某些场合并不能传输或者储存二进制流。比如，如果一个传输协议是基于ASCII文本的，那么它就不能传输二进制流，那你要将二进制流传输就得编码。常见的诸如HTTP协议的URL就是纯文本的，不能直接放二进制流](https://www.zhihu.com/question/36306744) [1](https://www.zhihu.com/question/36306744)。

[在ANSI码转Base64过程中，末尾补齐的0不会影响到解码后的内容，这是因Base64编码过程是3个字符一组的进行，如果原文长度不是3的倍数，后面的位都会用0来补充。在解码时，这些补充的0会被忽略，因此不会影响到解码后的内容](https://zhuanlan.zhihu.com/p/384238870) [2](https://zhuanlan.zhihu.com/p/384238870)。

[关于如何知道ANSI码转Base64过程中末尾补齐了多少个0，这可以通过观察Base64编码后的字符串来判断。如果Base64编码后的字符串末尾有一个"=“，那么说明在编码过程中补充了两个0；如果有两个”="，那么说明补充了四个0](https://zhuanlan.zhihu.com/p/384238870) [2](https://zhuanlan.zhihu.com/p/384238870)。

如果你想进一步了解 Base 64 编码的原理和更多知识，你可以参考以下资源：

1. [让你彻底理解Base64算法（Base64是什么，Base64解决什么问题，Base64字符串末尾的=是什么）](https://zhuanlan.zhihu.com/p/384238870)
2. [为什么要使用base64编码，有哪些情景需求？](https://www.zhihu.com/question/36306744)
3. [为什么要做base64编码](https://zhuanlan.zhihu.com/p/371596658)
4. [Base64 编码/解码 | 菜鸟工具](https://c.runoob.com/front-end/693/)

这些资源都提供了详细的 Base 64 编码的介绍和应用场景，可以帮助你更深入地理解 Base 64 编码。

[【Base64编码简述-哔哩哔哩】](https://b23.tv/ntmLon7)
## 网络上的免费 api 接口使用方法
以[小小api](https://xxapi.cn/) 为例
只要使用“请求示例”中代码在对应的语言环境下运行，就会得到接口返回的信息
如示例代码为：
```Python
import requests

url = "https://v2.xxapi.cn/api/heisi"
payload = {}
headers = {
'User-Agent': 'xiaoxiaoapi/1.0.0 (https://xxapi.cn)'
}
response = requests.request("GET", url, headers = headers, data = payload)
print(response.text)
```
那么根据返回示例：
```Python
{
	"code": 200,
	"msg": "数据请求成功",
	"data": "https://cdn.api-m.com/images/heisi/ZEaa0QkpkhlL8BWfavVJN78X.jpg"
}
```
最终 response 这个对象就是一个 dict（或者说 json），使用 Python 解析出 data 就会得到图片的地址，使用图片查看器打开即可
```Python
import requests
import os
import webbrowser  # 用于调用默认程序打开图片

# 1. 调用API获取图片URL
url = "https://v2.xxapi.cn/api/heisi"
response = requests.get(url)
result = response.json()

if result["code"] == 200:
    image_url = result["data"]  # 提取图片URL
    print("图片地址:", image_url)

    # 2. 下载图片到本地临时文件
    image_data = requests.get(image_url).content
    temp_path = os.path.join(
        os.environ["TEMP"], "xxapi_image.jpg"
    )  # 保存在系统临时目录
    with open(temp_path, "wb") as f:
        f.write(image_data)

    # 3. 用Windows图片查看器打开
    webbrowser.open(temp_path)  # 调用默认程序打开
else:
    print("API请求失败:", result["msg"])
```
说明文档中

| 参数名    | 传递参数 | 传入位置  | 类型  | 参数说明   |
| ------ | ---- | ----- | --- | ------ |
| return | 302  | query | 可选  | 重定向到图片 |
表示在 url 的 query 位置（拼接在 URL 的 "?“字符后的字段）如果填
```url
?return(参数名)=302(参数值)
```
就会得到对应的结果，比如填 302 表示"重定向到图片"，那么就不用手动解析 json，直接得到图片源文件（由浏览器或网络服务工具解析）
如果接口允许调试，多半是调试各种参数，这些参数填 url 的对应位置会有不同结果
```plain
https://example.com/path/to/api?key1=value1&key2=value2
\___/   \________/\________/ \________________________/
  |          |         |                 |
协议         域名      路径               查询
```
- **`?` 是查询参数的起始符号**，后的所有内容均为 `query` 参数。
- **`&` 分隔参数**：多个参数用 `&` 连接（如 `?param1=A&param2=B`）。
- **浏览器/客户端的隐含规则**：所有通过 URL 传递的参数，必须遵循 `?` 开头、`&` 分隔的格式，否则会被视为路径的一部分。

| 参数值                 | 作用                         | 返回示例                          | 适用场景                |
| ------------------- | -------------------------- | ----------------------------- | ------------------- |
| **不传** 或 `空`        | 返回 JSON 格式数据                 | `{"code":200,"data":"图片URL"}` | 需要解析 URL 后再处理（如前端展示）  |
| `302`               | **强制跳转**到图片直链（HTTP 302 重定向） | **直接显示图片**（不会返回 JSON）          | 需要快速查看图片（如浏览器地址栏调用） |
| `json` (或任意非"302"值) | 等价于"不传"，返回 JSON             | 同默认行为                         | 无特殊需求时用默认即可         |
