# 基本语法
## C++语法
### C++11 新特性
- 尾置返回类型（Trailing Return Type）-> [[C++ Runoob Tutoral#decltype 关键字#工作原理]]
- C++14 引入 `decltype(auto)`
- lambda 特性-> [[C++ Runoob Tutoral#lambda 函数]]
- 静态变量/函数/成员-> [[C++ Runoob Tutoral#static 语义]] -> [[C++ Runoob Tutoral#静态成员和非静态成员]]
## decltype 关键字和特性
![[C++ Runoob Tutoral#decltype 关键字]]
## const 关键字
![[C++ Runoob Tutoral#const 关键字]]
# 模板元编程基本知识
## 问题牵引出的知识
### 三目运算符
隐式要求：[[模板元编程#^u49hkz]]
### 行业最佳实践
模板元编程实现 Pair 和 for_each 支持
```cpp
// std::pair的简单实现
template <typename First, typename Second>
class Pair {
  public:
	Pair() = default;
	Pair(const First& f, const Second& s)
		: first_(f)
		, second_(s) {}
	Pair(First&& f, Second&& s)
		: first_(std::move(f))
		, second_(std::move(s)) {}

	bool operator==(const Pair& other) const { return other.first_ == this->first_ && other.second_ == this->second_; }

	bool operator<(const Pair& other) const {
		return this->first_ < other.first_;	 // std::pair 只需要写到这里即可
		// return this->first_ < other.first_ || (!(other.first_) && this->second_ < other.second_);
	}

	operator std::pair<First, Second>() const { return std::make_pair<First, Second>(first_, second_); }

	First  first_;
	Second second_;
};

template <typename First, typename Second>
Pair<First, Second> make_pair(First&& f, Second&& s) {
    return Pair<First, Second>(std::forward<First>(f), std::forward<Second>(s));
}

template <typename ElemType, size_t N>
class MinForeach {
  public:
	MinForeach(std::initializer_list<ElemType> init) { std::copy(init.begin(), init.end(), data_); }
	ElemType* begin() { return data_; }
	ElemType* end() { return data_ + N; }

  private:
	ElemType data_[N];
};

int main() {
	MinForeach<Pair<int, std::string>, 10> mf = {
		{ 0, "000" },
		  { 1, "111" },
		{ 2, "222" },
		  { 3, "333" },
		{ 4, "444" },
		{ 5, "555" },
		  { 6, "666" },
		{ 7, "777" },
		  { 8, "888" },
		{ 9, "999" }
	};
	for(const auto& item : mf) {
		std::cout << item.first_ << " " << item.second_ << std::endl;
	}
}
```
使用完美转发并将运算符重载函数设置为常函数是**不强制但是已成为行业最佳实践**
- 为什么 Pair 的万能引用类型参数的构造函数就需要使用 `std::move()`来把参数通过移动语义赋值到变量中？

> [!note]
> 因为
> ```cpp
> Pair(First&& f, Second&& s)
>     : first_(f)
>     , second_(s) {}
> ```
> 中，f和s虽然是万能引用对象，但他们本身有名字，所以是左值，这里使用 `std::move()` 减少拷贝开销
- 为什么重载 Pair 的 `==` 和 `<`符号时，参数类型是 const Pair&？这是 C++语法规定的吗？
> [!note]
> 这只是最佳实践，也可以不这样

自定义数据结构的 for-each 适配代码中：
- 为什么要定义这么多别名？
```cpp
    using value_type = ElemType;
    using reference = ElemType&;
    using const_reference = const ElemType&;
    using iterator = typename std::vector<ElemType>::iterator;
    using const_iterator = typename std::vector<ElemType>::const_iterator;
    using size_type = size_t;
```

> [!note]
> 行业规范，C++标准库这么做，是最佳实践

- 我注意到你使用std::vector的iterator用作自定义类的iterator，这样并没有说明要实现for-each的基本条件，如果我要实现自己数据结构的迭代方法，定义迭代器就必须要借助底层容器来实现（比如这里存储数据的是 `vector<ElemType>` 所以迭代器需要借助 `vector<ElemType>`）吗，如果底层数据是一个 C-style 数组呢？
- 从你的回答中看不出来，如果我想要让我自己的数据结构支持 for-each 循环至少应该满足什么条件

### 类型运算符
```cpp
operator std::pair<First, Second>() const {
    return std::make_pair(first, second);
}
```
C++中的类型转换：
- 如果有 A，B 两种类，要允许 A->B 的转化，有两种方法：
	- 构造函数转化，定义 `B::B(const A& a)` ，注意这只能允许 A->B
	- 定义 `A::operator B()` 或者 `A::operator=` 类型转换重载
	- 定义模板类转换 `template<typename T> operator T(){return static_cast<T>(value_);}`
- 并且这只是单向的，只能允许 A->B，且**三种方式都会在 `{}` 初始化，`=` 赋值，`()` 方式下触发**
> [!note]
> 这是在允许 Pair->std::pair 的**隐式**转化，这种写法叫做*类型转换运算符*
### decltype(auto)
![[C++ Runoob Tutoral#decltype 关键字]]
## 折叠表达式
![[Modern C++#Note：折叠表达式]]
## 基本 STL 算法
### 一、不修改序列的算法
#### 1. 查找算法
```cpp
// find - 查找元素
auto it = std::find(vec.begin(), vec.end(), 42);
// find_if - 条件查找
auto it = std::find_if(vec.begin(), vec.end(), 
                       [](int x){ return x > 10; });
// count - 计数
int cnt = std::count(vec.begin(), vec.end(), 42);
// count_if - 条件计数
int cnt = std::count_if(vec.begin(), vec.end(),
                       [](int x){ return x % 2 == 0; });
// binary_search - 二分查找（需有序）
bool found = std::binary_search(vec.begin(), vec.end(), 42);
// lower_bound/upper_bound - 边界查找
auto lb = std::lower_bound(vec.begin(), vec.end(), 42);
auto ub = std::upper_bound(vec.begin(), vec.end(), 42);
```
#### 2. 检查算法
```cpp
// all_of - 所有元素满足条件
bool all_even = std::all_of(vec.begin(), vec.end(),
                           [](int x){ return x % 2 == 0; });
// any_of - 任意元素满足条件
bool has_negative = std::any_of(vec.begin(), vec.end(),
                               [](int x){ return x < 0; });
// none_of - 没有元素满足条件
bool no_zero = std::none_of(vec.begin(), vec.end(),
                           [](int x){ return x == 0; });
```
#### 3. 比较算法
```cpp
// equal - 序列相等
bool same = std::equal(vec1.begin(), vec1.end(), vec2.begin());
// mismatch - 查找第一个不同点
auto [it1, it2] = std::mismatch(vec1.begin(), vec1.end(), vec2.begin());
// lexicographical_compare - 字典序比较
bool smaller = std::lexicographical_compare(
    vec1.begin(), vec1.end(), 
    vec2.begin(), vec2.end());
```
### 二、修改序列的算法
#### 1. 拷贝/移动算法
```cpp
// copy - 复制
std::copy(src.begin(), src.end(), dest.begin());
// copy_if - 条件复制
std::copy_if(src.begin(), src.end(), dest.begin(),
            [](int x){ return x > 0; });
// copy_n - 复制n个元素
std::copy_n(src.begin(), 5, dest.begin());
// move - 移动元素
std::move(src.begin(), src.end(), dest.begin());
// transform - 转换每个元素
std::transform(src.begin(), src.end(), dest.begin(),
               [](int x){ return x * 2; });
// transform - 双输入转换
std::transform(src1.begin(), src1.end(), src2.begin(), dest.begin(),
              [](int a, int b){ return a + b; });
```
#### 2. 填充/生成算法
```cpp
// fill - 填充值
std::fill(vec.begin(), vec.end(), 42);
// fill_n - 填充n个元素
std::fill_n(vec.begin(), 5, 42);
// generate - 使用生成器
std::generate(vec.begin(), vec.end(), 
              [](){ static int i = 0; return i++; });
// generate_n - 生成n个元素
std::generate_n(std::back_inserter(vec), 10,
                [](){ static int i = 0; return i++; });
// iota - 生成递增序列
std::iota(vec.begin(), vec.end(), 0);  // 0, 1, 2, 3...
```
#### 3. 交换算法
```cpp
// swap - 交换两个元素
std::swap(a, b);
// swap_ranges - 交换两个范围
std::swap_ranges(vec1.begin(), vec1.end(), vec2.begin());
// iter_swap - 交换迭代器指向的元素
std::iter_swap(it1, it2);
```
### 三、重新排列算法
#### 1. 排序算法
```cpp
// sort - 排序
std::sort(vec.begin(), vec.end());
// sort - 自定义比较
std::sort(vec.begin(), vec.end(), 
         [](int a, int b){ return a > b; });  // 降序
// stable_sort - 稳定排序
std::stable_sort(vec.begin(), vec.end());
// partial_sort - 部分排序
// 前n个元素排序，后面未排序
std::partial_sort(vec.begin(), vec.begin() + 5, vec.end());
// nth_element - 找到第n小的元素
auto middle = vec.begin() + vec.size()/2;
std::nth_element(vec.begin(), middle, vec.end());
```
#### 2. 划分算法
```cpp
// partition - 根据条件分区
auto it = std::partition(vec.begin(), vec.end(),
                        [](int x){ return x % 2 == 0; });
// it指向第一个不满足条件的元素
// stable_partition - 稳定分区
auto it = std::stable_partition(vec.begin(), vec.end(),
                               [](int x){ return x < 10; });
// partition_copy - 分区并复制
std::partition_copy(src.begin(), src.end(),
                    even.begin(), odd.begin(),
                    [](int x){ return x % 2 == 0; });
```
#### 3. 旋转/反转算法
```cpp
// reverse - 反转序列
std::reverse(vec.begin(), vec.end());
// reverse_copy - 反转并复制
std::reverse_copy(src.begin(), src.end(), dest.begin());
// rotate - 旋转序列
auto middle = vec.begin() + 3;
std::rotate(vec.begin(), middle, vec.end());
// rotate_copy - 旋转并复制
std::rotate_copy(src.begin(), middle, src.end(), dest.begin());
// shift_left/shift_right (C++20) - 移动元素
std::shift_left(vec.begin(), vec.end(), 2);  // 左移2位
```
### 四、删除/唯一化算法
#### 1. 删除算法
```cpp
// remove - 删除特定值
auto new_end = std::remove(vec.begin(), vec.end(), 42);
vec.erase(new_end, vec.end());
// remove_if - 条件删除
auto new_end = std::remove_if(vec.begin(), vec.end(),
                             [](int x){ return x % 2 == 0; });
// remove_copy - 删除并复制
std::remove_copy(src.begin(), src.end(), dest.begin(), 42);
// remove_copy_if - 条件删除并复制
std::remove_copy_if(src.begin(), src.end(), dest.begin(),
                   [](int x){ return x < 0; });
```
#### 2. 唯一化算法
```cpp
// unique - 删除相邻重复元素
auto new_end = std::unique(vec.begin(), vec.end());
// unique - 自定义相等判断
auto new_end = std::unique(vec.begin(), vec.end(),
                          [](int a, int b){ return abs(a-b) < 2; });
// unique_copy - 唯一化并复制
std::unique_copy(src.begin(), src.end(), dest.begin());
```
### 五、集合算法（需有序序列）
```cpp
// merge - 合并两个有序序列
std::merge(vec1.begin(), vec1.end(), 
          vec2.begin(), vec2.end(), 
          dest.begin());
// set_union - 并集
std::set_union(set1.begin(), set1.end(),
              set2.begin(), set2.end(),
              dest.begin());
// set_intersection - 交集
std::set_intersection(set1.begin(), set1.end(),
                      set2.begin(), set2.end(),
                      dest.begin());
// set_difference - 差集
std::set_difference(set1.begin(), set1.end(),
                    set2.begin(), set2.end(),
                    dest.begin());
// set_symmetric_difference - 对称差
std::set_symmetric_difference(set1.begin(), set1.end(),
                             set2.begin(), set2.end(),
                             dest.begin());
// includes - 包含测试
bool contains = std::includes(set1.begin(), set1.end(),
                             set2.begin(), set2.end());
```
### 六、堆算法
```cpp
// make_heap - 创建堆
std::make_heap(vec.begin(), vec.end());
// push_heap - 添加元素到堆
vec.push_back(42);
std::push_heap(vec.begin(), vec.end());
// pop_heap - 移除堆顶元素
std::pop_heap(vec.begin(), vec.end());
vec.pop_back();  // 移除最大元素
// sort_heap - 堆排序
std::make_heap(vec.begin(), vec.end());
std::sort_heap(vec.begin(), vec.end());  // 变为有序序列
```
### 七、数值算法
```cpp
// accumulate - 累加
int sum = std::accumulate(vec.begin(), vec.end(), 0);
int product = std::accumulate(vec.begin(), vec.end(), 1,
                             std::multiplies<int>());
// inner_product - 内积
int dot = std::inner_product(vec1.begin(), vec1.end(),
                            vec2.begin(), 0);
// partial_sum - 部分和
std::partial_sum(vec.begin(), vec.end(), dest.begin());
// 输入: 1 2 3 4
// 输出: 1 3 6 10
// adjacent_difference - 相邻差
std::adjacent_difference(vec.begin(), vec.end(), dest.begin());
// 输入: 1 3 5 7
// 输出: 1 2 2 2
// inclusive_scan/exclusive_scan (C++17) - 并行前缀和
std::inclusive_scan(vec.begin(), vec.end(), dest.begin());
// transform_reduce (C++17) - Map-Reduce操作
int result = std::transform_reduce(vec.begin(), vec.end(), 0,
                                  std::plus<>(),
                                  [](int x){ return x * x; });
```
### 八、排列算法
```cpp
// next_permutation - 下一个排列
bool has_next = std::next_permutation(vec.begin(), vec.end());
// prev_permutation - 上一个排列
bool has_prev = std::prev_permutation(vec.begin(), vec.end());
// is_permutation - 判断是否为排列
bool is_perm = std::is_permutation(vec1.begin(), vec1.end(),
                                  vec2.begin());
```
### 九、采样/洗牌算法
```cpp
// random_shuffle (C++14前) / shuffle (C++11起)
std::random_device rd;
std::mt19937 g(rd());
std::shuffle(vec.begin(), vec.end(), g);
// sample (C++17) - 采样
std::sample(population.begin(), population.end(),
           sample.begin(), 10, g);  // 随机采样10个
```
### 十、最小/最大算法
```cpp
// min/max - 最小/最大值
int m = std::min(a, b);
int m = std::max(a, b);
// minmax (C++11) - 同时获取最小最大值
auto [min_val, max_val] = std::minmax(a, b);
// min_element/max_element - 范围中最小/最大元素
auto min_it = std::min_element(vec.begin(), vec.end());
auto max_it = std::max_element(vec.begin(), vec.end());
// minmax_element - 同时找到最小和最大元素位置
auto [min_it, max_it] = std::minmax_element(vec.begin(), vec.end());
// clamp (C++17) - 限制在范围内
int clamped = std::clamp(value, 0, 100);  // 限制在0-100
```
### 十一、搜索算法
```cpp
// search - 查找子序列
auto it = std::search(vec.begin(), vec.end(),
                     pattern.begin(), pattern.end());
// search_n - 查找连续n个相同元素
auto it = std::search_n(vec.begin(), vec.end(), 3, 42);
// find_first_of - 查找任意匹配元素
auto it = std::find_first_of(vec.begin(), vec.end(),
                            targets.begin(), targets.end());
// find_end - 查找最后出现的子序列
auto it = std::find_end(vec.begin(), vec.end(),
                       pattern.begin(), pattern.end());
```
### 十二、特别实用的组合示例
#### 1. 删除特定元素
```cpp
// 删除所有负数的优雅写法
vec.erase(std::remove_if(vec.begin(), vec.end(),
                        [](int x){ return x < 0; }),
         vec.end());
```
#### 2. 统计满足条件元素
```cpp
// 统计大于10的元素数量
int count = std::count_if(vec.begin(), vec.end(),
                         [](int x){ return x > 10; });
```
#### 3. 转换并收集结果
```cpp
// 转换并存储到新容器
std::vector<int> result;
std::transform(vec.begin(), vec.end(),
              std::back_inserter(result),
              [](int x){ return x * 2; });
```
#### 4. 查找并修改
```cpp
// 查找并修改第一个大于10的元素
auto it = std::find_if(vec.begin(), vec.end(),
                      [](int x){ return x > 10; });
if (it != vec.end()) {
    *it = 42;  // 修改找到的元素
}
```
#### 5. 有序集合操作
```cpp
// 求两个向量的交集并排序
std::vector<int> intersection;
std::set_intersection(vec1.begin(), vec1.end(),
                     vec2.begin(), vec2.end(),
                     std::back_inserter(intersection));
```
### 十三、C++20 新增算法
```cpp
// ranges::views - 范围适配器（最实用）
auto even_squares = vec | std::views::filter([](int x){ return x % 2 == 0; })
                       | std::views::transform([](int x){ return x * x; });
// ranges::sort - 范围排序
std::ranges::sort(vec);
// shift_left/shift_right - 移动
std::ranges::shift_left(vec, 2);
// starts_with/ends_with - 前缀/后缀检查
bool starts = std::ranges::starts_with(vec, prefix);
bool ends = std::ranges::ends_with(vec, suffix);
// contains - 检查包含
bool has = std::ranges::contains(vec, 42);
```
### 总结：最常用的算法
按使用频率排序：
1. **sort** - 排序
2. **find/find_if** - 查找
3. **copy/copy_if** - 复制
4. **transform** - 转换
5. **remove/remove_if + erase** - 删除
6. **count/count_if** - 计数
7. **accumulate** - 累加
8. **for_each** - 遍历
9. **fill/generate** - 填充/生成
10. **partition** - 分区
**核心技巧**：大部分算法都遵循"输入范围-输出迭代器-谓词/操作"的模式，掌握这个模式就能快速学会使用任何 STL 算法。