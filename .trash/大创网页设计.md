我现在你帮我设计一个静态网页，

关于网页的样式设计你可以参考当前工作页面的LoginRegPage，但这个项目并没有完成。你需要帮助我完成这个项目。下面是我的要求：

1. 我们先从注册登录页面开始，完成一个完整的登录注册页面，可以通过当前工作目录中的LoginRegPage 补充代码完成
	1. 登录页面中的各种输入框，登录框配上相应的图标
	2. 能通过点击对应的按钮跳转到 item 文件对应的用户协议页面和 privacy 隐私政策页面，并按照 markdown 格式渲染文本内容作为一个页面
	3. 页面之间的跳转效果，组件之间的点击和鼠标悬浮动画可以参考 LoginRegPage.css 中的设计
	4. 网站主页 Logo 使用Imgs/Logo.svg
	5. 最终实现效果图可以参考 sampleImages\登录页面效果图.png 和sampleImages\注册页面效果图.png

---
你做得很不错！下面是我的修改意见
1. 在 Logo 右边使用微软雅黑字体，加粗大号字体添加网站标题“智能决策灌溉系统”
2. 我发现你的网页在浏览器代开后点击用户协议或者隐私政策时会让浏览器下载对应的文件，这是不对的。用户在注册页面点击用户协议和隐私政策时会打开一个新的页面专门用来显示 items.md 和privacy.md文件的内容，并用 markdown 渲染文本样式
3. 导航栏用白色背景，黑色文字，将导航栏中的原有内容
```plain
#首页
#关于
#服务
#联系我们
```
改为
```plain
#系统概述
#系统架构
#监控面板
#数据分析
#灌溉系统
#系统设置
```
但保留登录按钮
并对每一个导航栏按钮做一个跳转到对应页面的操作，虽然这些页面我还没有设计并实现，但是你可以创建文件并吸入基本的内容，等待我以后来精细化处理
4. 我发现在点击登录和注册页面的输入框后，输入框中的提示输入文字会上浮到输入框上方，但是上浮后的文字有一个白色的背景底色，这很不和谐，我希望他是透明的
5. 增加用户填写字段的检查逻辑，登录界面的邮箱和密码是必填项，注册页面中的用户名，邮箱，密码，确认密码是必填项，如果用户在对应页面中有任意必填项没有填写无法点击登录或者注册按钮并且弹出警告
6. 注册页面你还缺少了手机号码（必填），用户类型（必填）（用三个原点 checkbox 单选框选择选择用户身份是：农场主，技术人员，网站管理员三个其中之一）和农场名称（必填）



---

你的设计存在以下问题
1. 网站在点击注册按钮时，会出现需要填的项太多，导致注册页面不够长，顶部的“注册”字样和注册页面下方的“注册”按钮等内容都不可见，也无法通过鼠标滚轮滑动翻页。
2. 注册页面必填项比较多并且纵向排列，确实需要更多的空间，你可以通过调整注册页面框的大小，缩放字体大小或者增加鼠标滚轮反动功能让用户能看到完整的注册页面。


---

调整登录和注册页面的页面框位置 wrapper 的属性是哪一条？我来手动修改
你的设计存在一些问题，在登录页面点击注册时，登录转换到注册框会有一个展开的过程，展开导致页面不够长向下滑动，这就导致了背景图片也会向下滚动一点，这样看起开很不自然，我希望你调整这个问题

接着我们来设计并实现一个侧边栏菜单，下面是我对他的要求：
1. 在登录成功后，网页左边常驻一个侧边栏
2. 侧边栏分为两个板块，板块中细分子页面（括号中内容）：主菜单（控制面板，数据分析，灌溉控制，气象数据，作物管理）   系统配置，设备管理，用户管理，日志记录）。每一个子页面都有一个页面来实现，当用户点击任意一个子页面时就会跳转到对应的页面，但是用户的侧边栏和导航栏不发生变化。
3. 所有的子页面你都要可以创建对应的文件并写入基本的页面框架代码，后精细化修改
4. 最重要的是能实现完整的跳转功能，这些对应的子页面都用一个对应的文件实现，为了让项目结构清晰，逻辑自然，你需要整理工作目录中的文件结构，你可以修改文件的名称（对应的也需要在引用这些文件的代码中修改引用名称），创建文件夹来整理


现在我们来实现关于侧边栏的所有功能：
侧边栏中的内容有：
- 主菜单
	- 控制面板：
	  包含整个网站的各种各样模块的设置
	- 数据分析
	  展示智能灌溉系统的各种数据汇总信息，包含：
		- 当前温度，气候湿度，不同作物下的灌溉量
		- 近 15 日温度变化情况
	- 灌溉控制
	- 气象数据
	- 作物管理
- 系统配置
	- 设备管理
	- 用户管理
	- 日志记录