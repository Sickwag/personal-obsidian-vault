## 一. 单选题（共32题）

### 1. (单选题)（）是队列的基本操作。

- A. 对队列中的元素排序
- B. 在队列元素之间插入元素
- C. 删除队头元素
- D. 取出最近入队的元素

_我的答案:_ C:删除队头元素;

### 2. (单选题)

有队列Q和栈S，初始时，Q中元素从头开始依次为1,2,3,4,5,6，S为空。若仅允许以下三种操作：（1）出队并输出出队元素（2）出队并将出队元素入栈（3）出栈并输出出栈元素，则不能得到的输出序列为（）。

- A. 1,2,4,3,5,6
- B. 6,5,4,3,2,1
- C. 4,5,6,1,2,3
- D. 2,3,4,5,6,1

_我的答案:_ D:2,3,4,5,6,1;

### 3. (单选题)入栈顺序为P1, P2,…, Pn，出栈顺序为1,2,3,…,n，如果P3=1，则P1的值（）。

- A. 可能是2
- B. 不可能是2
- C. 一定是3
- D. 一定是2

_我的答案:_ A:可能是2;

### 4. (单选题)某队列允许在其两端进行入队操作，但仅允许在一端进行出队操作，若元素a,b,c,d,e依次入此队列后再进行出队操作，则不可能得到的出队序列是（）。

- A. d,b,c,a,e
- B. d,b,a,c,e
- C. e,c,b,a,d
- D. b,a,c,d,e

_我的答案:_ A:d,b,c,a,e;

### 5. (单选题)最不适合存放队列的链表是（）。

- A. 只带队首指针的普通双链表
- B. 只带队尾指针的循环双链表
- C. 只带队尾指针的循环单链表
- D. 只带队首指针的循环双链表

_我的答案:_ A:只带队首指针的普通双链表;

### 6. (单选题)链栈不同于顺序栈的优势之处在于（）。

- A. 通常不会出现栈满的情况
- B. 删除操作更容易实现
- C. 通常不会出现栈空的情况
- D. 插入操作更容易实现

_我的答案:_ A:通常不会出现栈满的情况;

### 7. (单选题)用P表示进栈操作，用D表示出栈操作。进栈顺序是abcd，为了得到bcda的出栈顺序，相应的P和D的操作序列为（）。

- A. PPDPDPDD
- B. PPPDPDDD
- C. PDPPDPDD
- D. PPDDPDPD

_我的答案:_ A:PPDPDPDD;

### 8. (单选题)循环队列存放在数组a[0…10]中。当前rear=3,front=9。则从队列中删除2个元素，再加入1个元素后，rear和front的值为（）。

- A. 2和7
- B. 4和0
- C. 5和10
- D. 5和0

_我的答案:_ D:5和0;

### 9. (单选题)设栈的初始状态为空，当字符序列"mp3"作为栈的输入时，输出长度为3，且可以用作C语言标识符的序列有（）个。

- A. 3
- B. 5
- C. 6
- D. 4

_我的答案:_ A:3;

### 10. (单选题)栈和队列具有相同的（）。

- A. 抽象数据类型
- B. 逻辑结构
- C. 运算
- D. 存储结构

_我的答案:_ B:逻辑结构;

### 11. (单选题)循环队列存放在数组Q[n]中。h指向头元素，t指向队尾元素的后一个位置。设队列中元素个数小于n，则队列中一共有（）个元素。

- A. (n+h-t)%n
- B. t-h
- C. (n+t-h)%n
- D. n+t-h

_我的答案:_ A:(n+h-t)%n;

### 12. (单选题)循环队列存放在数组a[15]中，front指向队头元素的前一个位置，rear指向队尾元素。当front=8, rear=3时，队列的长度为（）。

- A. 5
- B. 11
- C. 6
- D. 10

_我的答案:_ D:10;

### 13. (单选题)元素a,b,c,d,e依次入栈。在所有可能的出栈序列中，以d开头的序列个数是（）。

- A. 3
- B. 5
- C. 6
- D. 4

_我的答案:_ D:4;

### 14. (单选题)循环队列存放在数组a[0…m]中。end1指向队头元素，end2指向队尾元素的后一个位置。设队列两端都可以进行入队出队操作，且初始时为空。则判断队空和队满的条件是（）。

- A.
    
    队空：end2 == (end1 + 1) mod m;
    
    队满：end1 == (end2 + 1) mod m;
    
- B.
    
    队空：end1 == end2;
    
    队满：end1 == (end2 + 1) mod (m + 1);
    
- C.
    
    队空：end1 == (end2 + 1) mod m;
    
    队满：end2 == (end1 + 1) mod (m + 1);
    
- D.
    
    队空：end1 == end2;
    
    队满：end1 == (end2 + 1) mod m;
    

_我的答案:_ D:队空：end1 == end2; 队满：end1 == (end2 + 1) mod m;;

### 15. (单选题)（）不是栈的基本操作。

- A. 返回栈底元素
- B. 判断栈是否为空
- C. 将栈置为空栈
- D. 删除栈顶元素

_我的答案:_ B:判断栈是否为空;

### 16. (单选题)4 个不同元素依次进栈，能得到（）种不同的出栈序列。

- A. 14
- B. 13
- C. 16
- D. 15

_我的答案:_ A:14;

### 17. (单选题)设栈S和队列Q的初始状态为空，元素a,b,c,d,e,f依次进入栈S，各元素出栈后即进入队列Q。若出队序列为c,e,d,f,b,a，则栈S的容量至少为（）。

- A. 4
- B. 5
- C. 3
- D. 2

_我的答案:_ C:3;

### 18. (单选题)已知入栈序列为1,2,3,…,n，输出序列为p1,p2,p3,…,pn。若p1=n，则pi为（）。

- A. i-1
- B. n-i+1
- C. 不确定
- D. n-i

_我的答案:_ B:n-i+1;

### 19. (单选题)链式存储的队列在进行删除运算时（）。

- A. 头尾指针可能都要修改
- B. 只需要修改尾指针
- C. 头尾指针一定都要修改
- D. 只需要修改头指针

_我的答案:_ A:头尾指针可能都要修改;

### 20. (单选题)栈和队列的主要区别在于（）。

- A. 所含元素不同
- B. 存储结构不同
- C. 插入、删除规则不同
- D. 逻辑结构不同

_我的答案:_ C:插入、删除规则不同;

### 21. (单选题)对表达式求值时，使用运算数栈S来辅助计算。设S只有2个存储单元，则不会发生溢出的表达式是（）。

- A. a-b*(c+d)
- B. (a+b)/c-d
- C. (a-b)*(c-d)
- D. a*(b-c*d)

_我的答案:_ C:(a-b)*(c-d);

### 22. (单选题)链队中队头指针为front，队尾指针为rear，则将指针x指向的结点插入队列需要执行的操作是（）。

- A. x -＞ next = rear -＞ next; rear = x;
- B. rear -＞ next = x; rear = rear -＞ next
- C. front -＞ next = x; front = front -＞ next; x -＞ next = NULL
- D. x -＞ next = NULL; rear -＞ next = x; rear = rear -＞ next;

_我的答案:_ D:x -＞ next = NULL; rear -＞ next = x; rear = rear -＞ next;;

### 23. (单选题)顺序栈存放在数组a[n]中，top指向栈顶，top= -1表示栈空。在栈不满的情况下，元素x进栈的操作为（）。

- A. a[top++]=x
- B. a[++top]=x
- C. a[--top]=x
- D. a[top--]=x

_我的答案:_ B:a[++top]=x;

### 24. (单选题)链栈的结点表示为(data, link)，top指向栈顶，若想删除栈顶结点，并将删除结点的值保存到x中，则应执行操作（）。

- A. x = top -＞ data; top = top -＞ link
- B. x = top; top = top -＞ link
- C. x = top -＞ link
- D. top = top -＞ link; x = top -＞ link

_我的答案:_ A:x = top -＞ data; top = top -＞ link;

### 25. (单选题)循环队列存放在数组a[0…n]中。队列非空时front指向队头元素，rear指向队尾元素。初始时队列为空，要求第一个入队的元素存放在a[0]处，则front和rear的初始值为（）。

- A. n,n
- B. 0,0
- C. 0,n-1
- D. 0,n

_我的答案:_ C:0,n-1;

### 26. (单选题)一个空顺序栈的栈顶指针为top= -1，执行Push、Push、Pop、Push、Push、Pop、Pop、Push、Push操作后，栈顶指针的值为（）。

- A. 2
- B. 3
- C. 0
- D. 1

_我的答案:_ A:2;

### 27. (单选题)将编号为0和1的两个栈存放于同一个数组空间v[m]中，栈底分别处于数组的两端。0号栈的栈顶top[0]=-1时栈空；1号栈的栈顶top[1]=m时栈空。则判断此共享栈已满的条件是（）。

- A. top[0]==top[1]-1
- B. top[0]==top[1]+1
- C. top[0]+top[1]==m
- D. top[0]==top[1]

_我的答案:_ B:top[0]==top[1]+1;

### 28. (单选题)在递归过程调用时，为了保存返回地址，实际参数和局部变量，要求使用一种称为（）的数据结构。

- A. 向量
- B. 队列
- C. 栈
- D. 字符串

_我的答案:_ C:栈;

### 29. (单选题)链栈的结点表示为(data, next)，top指向栈顶。则插入结点x的操作为（）。

- A. x-＞next=top; top=x
- B. top-＞next=x
- C. x-＞next=top-＞next;top-＞next=x
- D. x-＞next=top; top=top-＞next

_我的答案:_ C:x-＞next=top-＞next;top-＞next=x;

### 30. (单选题)设a,b,c,d,e,f依次进栈，且进栈时允许出栈。则不可能得到的出栈序列是（）。

- A. bacfed
- B. dbcafe
- C. fedcba
- D. dcebaf

_我的答案:_ B:dbcafe;

### 31. (单选题)数组a[0…n]用来存放循环队列，则入队时的操作是（）。

- A. rear = (rear + 1) mod (n - 1)
- B. rear = (rear + 1) mod (n + 1)
- C. rear = (rear + 1) mod n
- D. rear = rear + 1

_我的答案:_ B:rear = (rear + 1) mod (n + 1);

## 二. 判断题（共19题）

### 32. (判断题)调用函数时，其局部变量一般存放在栈中。

- A. 对
- B. 错

_我的答案:_ 对

### 33. (判断题)链栈与顺序栈相比，更容易实现插入操作。

- A. 对
- B. 错

_我的答案:_ 对

### 34. (判断题)栈可以用于解决迷宫求解问题。

- A. 对
- B. 错

_我的答案:_ 对

### 35. (判断题)队列的"先进先出"特性是指每次从队列中删除的总是最早插入的元素。

- A. 对
- B. 错

_我的答案:_ 对

### 36. (判断题)只要确定了入栈顺序，就可以确定出栈顺序。

- A. 对
- B. 错

_我的答案:_ 错

### 37. (判断题)栈和队列的主要区别是插入和删除操作的限制不同。

- A. 对
- B. 错

_我的答案:_ 对

### 38. (判断题)解决同一个问题时，使用递归算法的效率一般高于非递归算法。

- A. 对
- B. 错

_我的答案:_ 错

### 39. (判断题)用链式存储的队列在进行删除操作时，只需要修改尾指针。

- A. 对
- B. 错

_我的答案:_ 错

### 40. (判断题)函数调用时，系统使用栈来保存必要信息。

- A. 对
- B. 错

_我的答案:_ 对

### 41. (判断题)消除递归只能使用栈。

- A. 对
- B. 错

_我的答案:_ 对

### 42. (判断题)栈是一种受限制的线性表，只允许在一端进行操作。

- A. 对
- B. 错

_我的答案:_ 对

### 43. (判断题)在循环队列中进行插入和删除时，无须移动队列中元素的位置。

- A. 对
- B. 错

_我的答案:_ 对

### 44. (判断题)栈的基本操作有判断栈是否为空、删除栈底元素、将栈置为空栈等。

- A. 对
- B. 错

_我的答案:_ 对

### 45. (判断题)对同一输入序列进行两组不同的入栈出栈组合操作，得到的输出序列也相同。

- A. 对
- B. 错

_我的答案:_ 对

### 46. (判断题)用单链表表示链式队列的队头在链表的链头位置。

- A. 对
- B. 错

_我的答案:_ 对

### 47. (判断题)栈和队列都是线性结构。

- A. 对
- B. 错

_我的答案:_ 对

### 48. (判断题)队列可以进行的操作有删除队头元素、向队列元素之间插入元素、对队列中元素排序等。

- A. 对
- B. 错

_我的答案:_ 对

### 49. (判断题)栈是限制存取点的非线性结构。

- A. 对
- B. 错

_我的答案:_ 对

### 50. (判断题)栈和队列具有相同的逻辑结构。

- A. 对
- B. 错

_我的答案:_ 对