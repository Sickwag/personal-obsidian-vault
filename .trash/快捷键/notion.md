### 一、基本操作快捷键
|   |   |
|---|---|
|功能|快捷键|
|新建页面|Ctrl+N|
|新建数据库|Ctrl+Alt+N|
|保存|Ctrl+S|
|撤销|Ctrl+Z|
|重做|Ctrl+Shift+Z或Ctrl+Y|
|复制|Ctrl+C|
|粘贴|Ctrl+V|
|剪切|Ctrl+X|
|全选|Ctrl+A|
|查找|Ctrl+F|
|插入时间|**Alt+Shift+T**|
### 二、编辑快捷键
|   |   |
|---|---|
|功能|快捷键|
|加粗|Ctrl+B|
|标题|Ctrl+Alt+1至Ctrl+Alt+6|
|斜体|Ctrl+I|
|删除线|Ctrl+Shift+X|
|代码块|Ctrl+Shift+C|
|增加缩进|Tab|
|减少缩进|Shift+Tab|
|选择所有匹配项|Ctrl+Alt+L|
### 三、视图快捷键
|   |   |
|---|---|
|功能|快捷键|
|切换侧边栏|Ctrl+\\|
|切换全屏模式|F11|
|刷新|Ctrl+R|
|移动到上一个页面|Ctrl+↑|
|移动到下一个页面|Ctrl+↓|
### 四、[数据库](https://www.zhihu.com/search?q=%E6%95%B0%E6%8D%AE%E5%BA%93&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22answer%22%2C%22sourceId%22%3A%222982055737%22%7D)快捷键
|   |   |
|---|---|
|功能|快捷键|
|插入行|Ctrl+Alt+↑|
|插入行|Ctrl+Alt+↓|
|插入列|Ctrl+Alt+←|
|插入列|Ctrl+Alt+→|
|删除行|Ctrl+Shift+D|
|删除列|Ctrl+Shift+Alt+D|
作者：Fan思路
链接：https://zhuanlan.zhihu.com/p/402175501
来源：知乎
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。
- 按 `cmd/ctrl` + `n` 以创建新页面（仅限桌面应用）。
- 按 `cmd/ctrl` + `shift` + `n` 或 `cmd/ctrl` + `click` 单击以打开一个新的 Notion 窗口。
- 按 `cmd/ctrl` + `p` 以打开搜索或跳转到最近查看的页面。
- 按 `cmd/ctrl` + `[` 退回 a page.
- 按 `cmd/ctrl` + `]` 前进 a page.
- 按 `cmd/ctrl` + `shift` + `l` 切换到黑暗模式（这是 L 而不是 1）。

> 提示：
## Markdown 编辑语法
**键入时：**
- 键入 `*` 在文本两侧以加粗
- 键入 文本两侧将其变为斜体
- 键入 ~`~在你的内联字符. (这是1键左侧的符号)
- 键入 `~` on either side of your text to 删除线
**在任何新行或内容块的开头，尝试下以下内容:**
- 键入 , , 或 `+` 跟着按 `space` 创建一个[子弹笔记列表](https://www.zhihu.com/search?q=%E5%AD%90%E5%BC%B9%E7%AC%94%E8%AE%B0%E5%88%97%E8%A1%A8&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22402175501%22%7D)；
- 键入 `[]` 创建一个 [to-do checkbox](https://www.zhihu.com/search?q=to-do%20checkbox&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22402175501%22%7D). (这里不需要 `space`)；
- 键入 `1.` 跟着按 `space` 创建一个序列列表；
- 键入 `#` 跟着按 `space` 创建一个 H1 一级标题
- 键入 `##` 跟着按 `space` 创建一个 H2 二级标题 以此类推（最多3级）
- 键入 `>` 键入 `#` 跟着按 `space` 创建一个 toggle列表
- 键入 `"` 跟着按 `space` 创建一个引用字段
选择文本后，按 cmd/ctrl + b 到粗体文本。
选择文本后，按 cmd/ctrl + i 以将文本意在化。
选择文本后，按 cmd/ctrl + u 以下划线文本。
选择文本后，按 cmd/ctrl +shift +s 进行~~删除线~~。
选定文本后，按 cmd/ctrl + k 添加链接。您也可以在选定的文本上粘贴 URL，以便使用 cmd/ctrl+v 将其转为链接。
选定文本后，按 cmd/ctrl + e 进行内联代码。
按标签以凹嵌和嵌套内容。注意：每当你凹嵌时，你都会在它上面的方块内筑巢。如果您选择父级，则会选择其下方的一切。例如：
在块的开头或末尾键入 / turn，将其转为不同类型的块。你会看到一个选择列表弹出。例如，下面介绍如何将文本行变成标题，然后变成要做的事情：
[![](https://pic2.zhimg.com/v2-e43d24a271f85abc23c730e745e79e1d_b.jpg)](https://pic2.zhimg.com/v2-e43d24a271f85abc23c730e745e79e1d_b.jpg)
在任何文本块的开头或末尾键入 / 颜色以更改其颜色或突出显示颜色。（要删除颜色或高亮显示，只需键入 / 默认。）示例： / 蓝色 / 蓝色背景
- 键入 `/color` 在一行开头或结尾来改变本行的颜色（ color or highlight color.）(移除`/default`.) 示例: `/blue`, `/blue background`
[![](https://pic3.zhimg.com/v2-920c679ce499ded8e18c7bf671356e22_b.jpg)](https://pic3.zhimg.com/v2-920c679ce499ded8e18c7bf671356e22_b.jpg)
对于所有这些内容创建快捷方式，cmd + option是 Mac 的组合，ctrl + 移位是 Windows 和 Linux 的组合：
按 cmd/ctrl + option / shift+ 0 以创建文本。
按 cmd/ctrl + option / shift+ 1 以创建 H1 标题。
按 cmd/ctrl + option / shift+ 2 以创建 H2 标题。
按 cmd/ctrl + option / shift+ 3 以创建 H3 标题。
按 cmd/ctrl + option / shift+ 4 以创建一个要做的事情复选框。
按 cmd/ctrl + option / shift+ 5 以创建[子弹列表](https://www.zhihu.com/search?q=%E5%AD%90%E5%BC%B9%E5%88%97%E8%A1%A8&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22402175501%22%7D)。
按 cmd/ctrl + option /shift+ 6 以创建编号列表。
按 cmd/ctrl + option / shift+ 7 以创建切换列表。
按 cmd/ctrl + option / shift+ 8 以创建代码块
看起来像这样：
`任何类型的代码都可以到这里复制到您的剪贴板上！`
按 cmd/ctrl + 选项 / shift+ 9 以创建新页面，或将上面的任何内容转换为页面。
- 按住 `cmd/ctrl` + `+` 放大
- 按住 `cmd/ctrl` + 缩小
按 `cmd/ctrl + shift+ u` 在页面层次结构中向上一个级别。
**在拖放时按住** `**alt**`**键，复制 Notion 页面上的任何内容：**
[![](https://pic3.zhimg.com/v2-274a9c32f2202f951f23f5f6b17ecbee_b.jpg)](https://pic3.zhimg.com/v2-274a9c32f2202f951f23f5f6b17ecbee_b.jpg)
## 编辑和移动块
[notion](https://www.zhihu.com/search?q=notion&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22402175501%22%7D)中的一切都是一个块——从一行类型（或段落）到一个图像或嵌入。这些快捷键允许你在选择整个块后编辑它们。
- 按`esc`选择您当前所在的块。或者清除选定的块。
- 按`cmd/ctrl+a`一次选择光标所在的块。
- 按空格以全屏打开选定图像。或退出全屏。
- 按箭头键选择不同的块。
- 按住`Shift`+“`向上/向下箭头键`”向上或向下扩展您的选择。
- 使用`cmd`+`Shift`+`点击`在Mac上，和'`ALT`+`Shift`+`点击`'在Windows/Linux上选择或取消选择整个块。
- 使用`Shift`+单击选择另一个块和所有块之间。
- 按退格或删除删除选定的块。
- 按`cmd/ctrl`+`d`复制您选择的块。
- 按回车键编辑选定块内的任何文本（或打开页面内的页面）。
- 按`cmd/ctrl` + `/` 编辑或更改一个或多个选定的块。
- 使用此快捷方式更改块的类型、颜色，或者编辑、复制或移动块。你会在弹出的菜单顶部看到一个文本框。只需键入你想要的操作、块或颜色：
这些快捷方式允许您在选择完整块后编辑它们。
**按** `**esc**` **以选择您当前所处于的区块。或清除选定的方块。**
**按住** `**cmd/ctrl**` **+** `**A**` **一次选择全部行或块**
`**向下移动**`**+** `**向上 / 向下箭头键**`**以向上或向下扩展您的选择。**
**按住** `**space**` **全屏展示图片.再按一下 退出全屏。**
**使用**`**cmd + shift**`**+**`**单击**` **（Mac），点击** `**alt**`**+**`**shift**`**+** `**单击**` **（Windows/Linux） 以选择或取消选择整个块。**
使用 `shift` + `click` 选择所有块中间的另外一个块。
按输入以编辑选定块内的任何文本（或打开页面内的页面）。
按 `cmd/ctrl` + `/` 编辑或更改一个或多个选定的块。
使用此快捷方式更改块的类型、颜色，或编辑、复制或移动块。您将在弹出的菜单顶部看到一个文本框。只需键入您想要的动作、块或颜色：
[![](https://pic1.zhimg.com/v2-52321ef0e7ed883628e0cbee4b92ed20_b.jpg)](https://pic1.zhimg.com/v2-52321ef0e7ed883628e0cbee4b92ed20_b.jpg)
在看板视图中，选择多个卡片，然后使用 `cmd/ctrl` +`/` 一次全部编辑。示例：以下是您如何一次更改多个任务的状态。
在板视图中，选择多张卡片，然后使用`cmd/ctrl+/`一次编辑它们。示例：下面是如何一次更改几个任务的状态。
[![](https://pic4.zhimg.com/v2-48dcfeeabe6bde248297300e90adbc13_b.jpg)](https://pic4.zhimg.com/v2-48dcfeeabe6bde248297300e90adbc13_b.jpg)
- 按住`cmd/ctrl`+`Shift`+`箭头键`移动选定的块。
- 按`cmd/ctrl`+`Option/ALT`+`t`展开或关闭切换列表中的所有切换。
- 按`cmd/ctrl`+`Shift`+`h`应用您使用的最后一个文本或突出显示颜色。
- 按`cmd/ctrl`+`回车键`来修改您当前所在的块。修改意味着：
- 打开一个页面
- 选中或取消选中待办事项复选框
- 打开或关闭切换列表项
- 使嵌入或图像全屏
**保持** `**cmd/ctrl**`**+** `**shift**` **+**`**箭头键**`**以移动选定的方块。**
按`cmd/ctrl` + `Option / alt` +`t` 以扩展或关闭切换列表中的所有切换。
按 `cmd/ctrl` +`shift`+`h` 以应用您使用的最后文本或突出显示颜色。
按 `cmd/ctrl` + 输入以修改您当前的块。修改方法：
**提到一个人** - 键入 `@`和另一个工作空间成员的名字， 以引起他们对某事的关注。他们会被通知的在评论和讨论中很有用。
**提及一页** - 键入工作空间中的另一页名称以创建指向该页面的链接。如果您更改了页面的名称，此链接也会自动更改。
**提及日期** -键入`\``+` 和任何格式的日期 （或 “昨天”， “今天” 或 “明天”， 甚至 “下周三”） 。有助于给自己定到期日。
**添加提醒** - 类型 `@remind`然后是任何格式的日期（包括 “昨天”、“今天”、“明天” 等）。您可以单击显示要调整要提醒的日期和确切时间的链接。您将在该时间收到通知。
**链接页面** - 键入`[`和工作空间中另一页的名称以创建指向该页面的链接。如果您更改了页面的名称，此链接也会自动更改。
**创建子页面** - 键入`[`和您要嵌入当前页面中的子页的名称。使用光标或箭头键选择 + 在显示的下拉中添加新的子页面。
**在别的地方创建新的页面** - 键入`[`和要创建的页面的名称。使用光标或箭头键选择在显示的下拉中添加新页面，然后选择您希望添加该页面的页面或数据库。
**创建子页面** - 键入`+`以及您要嵌入当前页面的子页面的名称。使用光标或箭头键选择 + 在显示的下拉中添加新的子页面。
**在别的地方创建新的页面** - 键入`+`以及要创建的页面的名称。使用光标或箭头键选择在显示的下拉中添加新页面，然后选择您希望添加该页面的页面或数据库。
**链接页面** - 键入 `+` 以及工作空间中另一页的名称，以创建指向该页面的链接。如果您更改了页面的名称，此链接也会自动更改。

> 提示： 当您使用
> 
> ```Plain
> [[
> ```
> 
> ```Plain
> +
> ```
按 `/`在 Notion 中，您可以选择插入内容块的完整菜单。您可以通过按 / 然后键入您想要的来进一步缩短这一点。以下是您的选择：
`esc`清除`/`菜单。很方便， 如果你只是想使用 `/`在你的写作。
以上任何一项也可以创建 - 完整而不是内联打开该类型的数据库视图在其自己的页面。例如：`/ full table`，`/full broard`
`/ 链接`创建一个链接的数据库 - 现有数据库的副本，您可以插入到任何页面在线和过滤或查看任何您想要的。有助于在同一页面上创建来自同一数据库的多个摘录。
`/ 图像`将提出上传或嵌入图像的选项，或添加一个从[未喷出](https://link.zhihu.com/?target=http%3A//unsplash.com/)。
`/pdf`允许您将 URL 粘贴到任何 PDF 中，以便它会在页面上在线显示。
`/ book`书可以让你粘贴在网址到任何网站创建一个[网络书签](https://www.zhihu.com/search?q=%E7%BD%91%E7%BB%9C%E4%B9%A6%E7%AD%BE&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22402175501%22%7D)。喜欢这个：
`/视频`允许您上传视频文件或嵌入来自 YouTube、Vimeo 等的视频。
`/ 音频`允许您上传音频文件或嵌入来自声云、Spotify 等的录音。
`/ 代码`创建一个代码块，您可以在那里编写和复制任何代码片段。
`/面包`插入面包屑菜单，显示当前页面位于您的工作空间中的位置。
`/ math`或 `/ tex`可以让你使用[TeX](https://link.zhihu.com/?target=https%3A//www.math.brown.edu/~jhs/ReferenceCards/TeXRefCard.v1.5.pdf)写数学方程和符号。
## @命令
### 提到一个人
- 键入`@`和另一个工作区成员的名字来引起他们对某事的注意。他们会得到通知。在评论和讨论中很有用。
### 请提及页面
- 键入`@`和工作区中另一个页面的名称，以创建指向该页面的链接。如果更改页面名称，此链接也会自动更改。
### 提到日期
- 键入`@`和任何格式的日期（或“昨天”、“今天”或“明天”，甚至“下周三”）。有助于给自己截止日期。
### 添加提醒
- 键入`@remind`，然后是任何格式的日期（包括“昨天”、“今天”、“明天”等）。你可以点击出现的链接来调整你想要被提醒的日期和确切时间。那时你会收到通知。
- 如果您只想键入`@`，请单击`esc`以取消`@`命令菜单。
## [[ 命令
### 链接页面
- 键入“`[[`”和工作区中另一个页面的名称来创建链接。如果您更改页面名称，该链接也会自动更改。
### 创建子页面
- 键入`[[`和要嵌套在当前页面中的子页面的名称。使用光标或箭头键在出现的下拉菜单中选择+添加新子页面。
### 在其他地方创建新页面
- 键入“`[[`”和要创建的页面名称。使用光标或箭头键选择“`↗在...`”在出现的下拉菜单中添加新页面，然后选择要添加该页面的页面或数据库。
## +命令
### 创建子页面
- 键入`+`和要嵌套在当前页面中的子页面的名称。使用光标或箭头键在出现的下拉菜单中选择`+`添加新子页面”。
### 在其他地方创建新页面
- *键入`+`和要创建的页面名称。使用光标或箭头键在出现的下拉菜单中选择↗添加新页面，然后选择要添加该页面的页面或数据库。
### 链接页面
- 键入“`+`”和工作区中另一个页面的名称来创建链接。如果您更改页面名称，该链接也会自动更改。
**提示**：当你使用`[[`时，下拉菜单会先显示**页面链接**选项；当你使用`+`时，下拉菜单会先显示**页面创建**选项。
## /命令
在概念中按`/`会出现一个完整的内容块菜单，您可以选择插入。您可以通过按`/`，然后键入您想要的内容来进一步快捷化。以下是您的选择：
### 基本
- `/text`或`/plain`明'创建一个新的文本块。
- `/page`创建一个新页面（当您按'回车'时，它会自动打开）
- `/bullet` 子弹笔记列表
- `/num`创建一个编号列表。
- `/todo`创建一个带有复选框的待办事项列表
- `/toggle`创建一个切换列表。
- `/div`创建一个浅灰色分隔线。
- `/quote`报价'创建一个较大文本的报价块。
- `/h1`或`/#` 创建一个大标题。
- `/h2` 或 `/##`创建一个中等大小的标题。
- `/h3`或 `/###` 创建一个小标题。 `/link`创建到工作区中另一个页面的链接。
- `esc` 清除 `/`菜单。如果你只是想在写作中使用`/`，这很方便。
### 内联
- `/mention`允许您提及工作区中的页面或人员
- `/date` `/reminder`允许您添加时间戳或提醒到您的页面
- `/equation`允许您添加符合您的文本的TeX公式
- `/emoji`显示表情符号选择器
### 数据库
- `/table-inline`在当前页面中创建一个数据库表。
- `/board-inline`在当前页面中创建看板。
- `/calendar-inline` 在当前页面中创建日历。
- `/list-inline`在当前页面中创建列表样式的数据库。
- `/gallery-inline`在当前页面中创建一个图库。
- `/timeline-inline`在当前页面中创建时间线。
- 上面的任何一个也可以用“-ful”代替“-inline”来创建，以自动在自己的页面中打开该类型的数据库视图。例如：`/table-full`, `/board-full`
- `/`[`linked`](https://www.zhihu.com/search?q=linked&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22402175501%22%7D)创建一个链接数据库-一个现有数据库的副本，您可以将其插入到任何页面中，并按照您想要的方式过滤或查看。有助于在同一页面上创建来自同一数据库的多个摘录。
### 媒体
- `/Images`将显示上传或嵌入图像的选项，或者从[Unplash]（[http://unsplash.com](https://link.zhihu.com/?target=http%3A//unsplash.com/)）中添加一个。
- `/pdf`允许您将URL粘贴到任何PDF，以便在您的页面上在线显示。
- `/book`允许你粘贴任何网站的网址来创建一个网络书签
- `'/video'`允许您上传视频文件或嵌入来自YouTube、Vimeo等的视频。
- `'/dio'`允许您上传音频文件或嵌入SoundCloud、Spotify等的录音。
- '`/code`'创建一个代码块，您可以在其中编写和复制任何代码片段。
- '`/file`'允许您从计算机上传任何文件或创建嵌入。
- '`/embed`'允许您添加使用Notion的500多个嵌入中的任何一个。
- `/comment` lets you create a comment on any block.
- `/duplicate`创建当前块的精确副本。
- `/moveto`允许您将该块移动到不同的页面。
- `/delete`删除当前块。
- `/toc`创建一个目录块。
- `/button` or `/template`'/按钮'或'/模板'为您提供了一个模板按钮，可以复制您定义的任何块的组合。
- `/bread`插入面包屑菜单，显示当前页面在工作区中的位置。
- `/math` 或 `/latex`'/数学'或'/latex'允许您使用[TeX]编写数学方程和符号（[https://www.math.brown.edu/~jhs/ReferenceCards/TeXRefCard.v1.5.pdf](https://link.zhihu.com/?target=https%3A//www.math.brown.edu/~jhs/ReferenceCards/TeXRefCard.v1.5.pdf)).
编辑于 2023-02-02 17:26
赞同 142
  
8 条评论
收藏
分享
举报
收起
## Notion使用教程7：Notion 快捷键分类汇总
[![](https://pic1.zhimg.com/80/v2-1948e53981b5cfe86fe90ec03ecf200f_200x0.jpg?source=4e949a73)](https://pic1.zhimg.com/80/v2-1948e53981b5cfe86fe90ec03ecf200f_200x0.jpg?source=4e949a73)
**组件世界(NotionPet)**：介绍：以 _Notion_ 为主题的数字化工具成长社区。 Notion 使用教程合辑 更多 Notion 使用教程系列专文，参见下文。其中，包括了_Notion_ 下载、汉化方案、块编辑器/Dat...
阅读全文
赞同 12添加评论2023-07-13
## 【Notion教程】学习 Notion 快捷键
[![](https://pic1.zhimg.com/50/v2-bbc736ddee49c06b7bbb857746b47246_l.jpg?source=4e949a73)](https://pic1.zhimg.com/50/v2-bbc736ddee49c06b7bbb857746b47246_l.jpg?source=4e949a73)
低调的学长
精通各类AI工具
_Notion安装包,Notion教程，Notion注册，Notion会员，Notion模板，Notion教程，Notion AI申请，Notion AI写作，Notion插件_
Notion 支持全键盘操作以及 Markdown 快捷键，所以你不用鼠标即可以做任何事情
**注意：**下面的内容你将频繁看到 `cmd/ctrl` 键。这个表示 Mac 上的 `command` 键和 Windows 以及 Linux 的 `control` 键。这些使用说明是面向 English/QWERTY 布局的键盘使用者，当然我们正在推进国际化！
## **通用的快捷键**
如果你不是一个[快捷键](https://www.zhihu.com/search?q=%E5%BF%AB%E6%8D%B7%E9%94%AE&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)爱好者，我们理解。如果你可以记住下面几个简单的命令，那么你仍然可以像一个 Notion 高级用户一样在 Notion 上进行操作
- 按 `cmd/ctrl` + `n` 来创建新页面（仅适用于桌面应用，包括浏览器 Notion 网页和桌面端应用程序）
- 按 `cmd/ctrl` + `shift` + `n` 或者使用 `cmd/ctrl` 然后`点击` 来创建一个新的 Notion 窗口
- 按 `cmd/ctrl` + `p` 来打开[搜索框](https://www.zhihu.com/search?q=%E6%90%9C%E7%B4%A2%E6%A1%86&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)，或者跳转到最近浏览的页面
- 按 `cmd/ctrl` + `[` 后退历史页面
- 按 `cmd/ctrl` + `]` 进入前一个历史页面
- 按 `cmd/ctrl` + `shift` + `l` 进行深色模式切换（l 是字母 L，而不是数字 1）
**提示：**你可以在 Notion 页面或者电脑其他任何地方使用表情选择器来进行[行内表情](https://www.zhihu.com/search?q=%E8%A1%8C%E5%86%85%E8%A1%A8%E6%83%85&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)输入！下面是不同平台调用的快捷键：
Mac: `ctrl` + `cmd` + `空格`
Windows 10: `windows key` + `.` 或者 `windows key` + `;`
## **Markdown 样式快捷键**
**在输入时：**
- 在文本两边输入 `*` 可以**加粗文本**
- 在文本两边输入 可以获得[_斜体样式文本_](https://www.zhihu.com/search?q=%E6%96%9C%E4%BD%93%E6%A0%B7%E5%BC%8F%E6%96%87%E6%9C%AC&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)
- 在文本两边输入 ` `` 可以创建 `[内联代码](https://www.zhihu.com/search?q=%E5%86%85%E8%81%94%E4%BB%A3%E7%A0%81&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)`（` 是在键盘底部数字 1 键左边的键）
- 在文本两本输入 `~` 可以获得[删除线样式文本](https://www.zhihu.com/search?q=%E5%88%A0%E9%99%A4%E7%BA%BF%E6%A0%B7%E5%BC%8F%E6%96%87%E6%9C%AC&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)
**在新的或者已经存在的内容块前：**
- 输入 、 或者 `+`，然后按 `空格`，可以创建一个无序列表
- 输入 `[]` 可以创建一个[待办清单](https://www.zhihu.com/search?q=%E5%BE%85%E5%8A%9E%E6%B8%85%E5%8D%95&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)（[ 和 ] 之间没有`空格`）
- 输入 `1.` 然后按 `空格` 可以创建一个有序列表
- 输入 `#` 然后按 `空格` 可以创建一个一级标题
- 输入 `##` 然后按 `空格` 可以创建一个[二级标题](https://www.zhihu.com/search?q=%E4%BA%8C%E7%BA%A7%E6%A0%87%E9%A2%98&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)
- 输入 `###` 然后按 `空格` 可以创建一个[三级标题](https://www.zhihu.com/search?q=%E4%B8%89%E7%BA%A7%E6%A0%87%E9%A2%98&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)
- 输入 `>` 然后按 `空格` 可以创建一个折叠列表
- 输入双引号 `"` 然后按 `空格` 可以创建一个引用块
## **创建和风格化内容的快捷键**
- 按 `enter` 可以插入一行文本
- 按 `shift` + `enter` 可以创建一个换行的文本块
- 按 `cmd/ctrl` + `shift` + `m` 可以创建唤起评论弹窗
- 输入 `—-` 可以创建一个分割线（连续三个破折号 -）
- 选中文本，按 `cmd/ctrl` + `b` 可以**加粗文本**
- 选中文本，按 `cmd/ctrl` + `i` 可以获得_斜体样式文本_
- 选中文本，按 `cmd/ctrl` + `shift` + `s` 可以获得删除线样式文本
- 选中文本，按 `cmd/ctrl` + `k` 可以添加链接。你也可以在选中的文本上按 `cmd/ctrl` + `v` 快速粘贴[链接](https://www.zhihu.com/search?q=%E9%93%BE%E6%8E%A5&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)从而把文本转换为链接
- 选中文本，按 `cmd/ctrl` + `e` 可以把文本转为`内联代码`
- 按 `tab` 缩进。**注意：**缩进时，当前嵌套的块是基于上一级块进行缩进。如果选中父级块，那么该内容块下所有块都将被选中。示例如下：
    - 块 2 ← 嵌套在块 1 下
    - 块 1 ← 父级块
- 按 `shift` + `tab` 反向缩进
- 在块开头或者结尾输入 `/turn` 可以把它转换为其他类型的块 你会看到一个候选列表弹出。比如，下面是一个如何把文本先转换成标题，然后转换成待办事项的操作示例
[![](https://pic2.zhimg.com/v2-e43d24a271f85abc23c730e745e79e1d_b.jpg)](https://pic2.zhimg.com/v2-e43d24a271f85abc23c730e745e79e1d_b.jpg)
- 在任何文本块开头或者结尾输入 `/color` 可以更改文本颜色或者背景色（要移除文本颜色或者背景色，只要输入 `/default`）比如：`/blue`，`/`[`blue background`](https://www.zhihu.com/search?q=blue%20background&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)
[![](https://pic3.zhimg.com/v2-920c679ce499ded8e18c7bf671356e22_b.jpg)](https://pic3.zhimg.com/v2-920c679ce499ded8e18c7bf671356e22_b.jpg)
对于创建内容的快捷键，`cmd` + `option` 是 Mac 上的组合键，`ctrl` + `shift` 是 Windows 和 Linux 上的组合键：
- 按 `cmd/ctrl` + `option/shift` + `0` 创建文本
- 按 `cmd/ctrl` + `option/shift` + `1` 创建一级标题
- 按 `cmd/ctrl` + `option/shift` + `2` 创建二级标题
- 按 `cmd/ctrl` + `option/shift` + `3` 创建三级标题
- 按 `cmd/ctrl` + `option/shift` + `4` 创建待办清单
- 按 `cmd/ctrl` + `option/shift` + `5` 创建无序列表
- 按 `cmd/ctrl` + `option/shift` + `6` 创建有序列表
- 按 `cmd/ctrl` + `option/shift` + `7` 创建折叠列表
- 按 `cmd/ctrl` + `option/shift` + `8` 创建代码块 - 效果如下：
`<b>任意类型的代码可以放这里并被复制到剪贴板！</b>`
- 按 `cmd/ctrl` + `option/shift` + `9` 创建一个新页面，或者把任一文本行转换成页面
- 按 `cmd/ctrl` + `+` 放大
- 按 `cmd/ctrl` + 缩小
- 按住 `option/alt` 然后拖拽任意内容进行复制
[![](https://pic3.zhimg.com/v2-274a9c32f2202f951f23f5f6b17ecbee_b.jpg)](https://pic3.zhimg.com/v2-274a9c32f2202f951f23f5f6b17ecbee_b.jpg)
## **编辑和移动块的快捷键**
Notion 中任意内容都是一个块 — 从一行文本（或者一个段落文本）到一张图片或者一个嵌入块。下面的快捷键可以让你在块选中时编辑整个块
- 按 `esc` 选中当前光标所在块。或者清除已选块
- 按 `cmd/ctrl` + `a` 全选当前光标所在块
- 按 `空格` 把所选图片以全屏形式打开。或者退出全屏
- 按 `箭头键` 可以选择其他块
- 按住 `shift` + `向上/向下` 可以向上/向下扩大选择范围
- 在 Mac 上使用 `cmd` + `shift` + `点击`，在 Windows/Linux 上使用 `option` + `shift` + `点击` 来选中或者取消选中块
- 使用 `shift` + `点击` 选择其他块或者所点击块之间的所有块
- 按 `后退` 或者 `删除` 来删除所选块
- 按 `cmd/ctrl` + `d` 来复制所选块
- 按 `enter` 对所选块进行编辑（或者在页面内打开页面）
- 按 `cmd/ctrl`+ `/` 对一个或者多个块进行编辑和更改
    - 使用这个快捷键可以更改块的类型、颜色或者进行编辑、复制或者移动块。你会看到弹出菜单有个文本输入框，在这里，请输入你想输入的动作、块名称或者颜色：
[![](https://pic1.zhimg.com/v2-52321ef0e7ed883628e0cbee4b92ed20_b.jpg)](https://pic1.zhimg.com/v2-52321ef0e7ed883628e0cbee4b92ed20_b.jpg)
- 在看板视图中，选中多张卡片，然后使用 `cmd/ctrl` + `/` 对它们同时进行编辑。比如：下面展示了如何同时更改多个任务的状态。
[![](https://pic4.zhimg.com/v2-48dcfeeabe6bde248297300e90adbc13_b.jpg)](https://pic4.zhimg.com/v2-48dcfeeabe6bde248297300e90adbc13_b.jpg)
- 按住 `cmd/ctrl` + `shift` + `箭头键` 对所选块进行移动
- 按 `cmd/ctrl` + `option/alt` + `t` 对折叠列表进行展开或者收起
- 按 `cmd/ctrl` + `shift` + `h` 对所选中文本应用最近一次文本颜色或者背景色的效果
- 按 `cmd/ctrl`+ `enter` 来修改当前光标所在块。修改有以下几层意思：
    - 打开页面
    - 勾选或者取消勾选待办清单
    - 展开或者关闭[折叠列表](https://www.zhihu.com/search?q=%E6%8A%98%E5%8F%A0%E5%88%97%E8%A1%A8&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)
    - 使嵌入块或者图片以全屏形式打开
## **@ 命令**
- **提及成员 -** 输入 `**@**` 和其他工作站成员名称来提醒他们一些事情。他们将收到通知。适用于评论和讨论场景
- **提及页面 -** 输入 `@` 和你工作站其他页面的标题可以创建指向这个页面的链接。如果你更改了页面标题，这个链接也会自动更改
- **提及日期 -** 输入 `@` 和任意形式的日期（可以是 "yesterday"，"today"，"tomorrow"，甚至可以是 "next Wednesday"）。适用于需要为自己设定交付日期的场景
- **添加提醒 -** 输入 `@remind` 和任意形式的日期（包括："yesterday"，"today"，"[tomorrow](https://www.zhihu.com/search?q=tomorrow&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)" 等）。你可以点击出现的链接，把日期调整成你想被提醒的精确时间）。你将在设定的时间点收到通知.
- 当你只想输入 @ 时，按 `esc` 来关闭 @-命令 的菜单
## **斜线命令**
在 Notion 中按 `/` 会唤起你可以选择或者插入的所有内容块的菜单。你可以按 `/` 然后输入任何你想添加的内容块的名字来更快添加此内容块。你可以选择：
## **基础块**
- `/text` 或者 `/plain` 创建文本块
- `/page` 创建页面（再按 `enter` 会自动帮你打开页面）
- `/bullet` 创建无序列表
- `/num` 创建有序列表
- `/todo` 创建有复选框的待办清单
- `/toggle` 创建折叠列表
- `/div` 创建浅灰色分隔线
- `/quote` 创建大号字体的引用块
- `/h1` 或 `/#` 创建一级标题
- `/h2` 或 `/##` 创建二级标题
- `/h3` 或 `/###` 创建三级标题
- `/link` 创建链接到你所在工作站其他页面的引用
- `esc` 关闭 / 唤起的菜单。当你协作中只想输入 / 时很有用
### **数据库**
- `/table-inline` 创建当前页面内嵌表格
- `/board-inline` 创建当前页面内嵌看板
- `/calendar-inline` 创建当前页面内嵌日历
- `/list-inline` 创建当前页面内嵌列表
- `/gallery-inline` 创建当前页面内嵌画廊
- 把上面命令中的 `inline` 改为 `full` 将自动在新页面创建相应的数据库类型。比如：`/table-full`，`/board-full`
- /linked 创建引用数据库 — 即已有数据库的副本，你可以把它插入到任意页面内行内，你可以对其进行内容进行筛选，或者浏览。适用于在同一页面创建基于同一数据库的多个视图（数据库中的视图概念）
## **媒体块**
- `/image` 将唤起一个用于上传图片、嵌入图片链接或者从 Unsplash 中添加[图片](https://www.zhihu.com/search?q=%E5%9B%BE%E7%89%87&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)链接的菜单
- `/pdf` 可以让你粘贴一个 PDF 文件链接，此文件将内嵌在页面内
- `/book` 可以让你把任何网站的链接创建为一个网页书签。像这样：
Notion - The all-in-one workspace for your notes, tasks, [wikis](https://www.zhihu.com/search?q=wikis&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D), and databases.
- `/video` 可以让你上传视频文件，或者内嵌来自 YouTube、Vimeo 等视频网站的视频
- `/audio` 可以让你上传音频文件，或者内嵌来自 SoundCloud、Spotify 等音频网站的音频
- `/code` 创建代码块，这里你可以编写或者复制代码片段
- `/file` 可以让你上传你电脑里面的任何文件，并内嵌在页面内
- `/embed` 可以让你添加 Notion 支持的 500+ 嵌入应用。
## **高级块**
- `/comment` 可以让你创建当前块的评论
- `/duplicate` 可以让你创建当前块的副本
- `/moveto` 可以让你把当前块移动到其他页面
- `/delete` 可以让你删除当前块
- `/toc` 可以让你创建当前页面的内容目录
- `/button` 或者 `/template` 可以让你复制一个用于你定义的组合块的模板按钮。
- `/bread` 可以让你插入[面包屑导航](https://www.zhihu.com/search?q=%E9%9D%A2%E5%8C%85%E5%B1%91%E5%AF%BC%E8%88%AA&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22article%22%2C%22sourceId%22%3A%22616140586%22%7D)，它可以让你知道你当前工作在工作站的哪个页面
- `/math` 或 `/latex` 可以让你使用 TeX 语法书写数学公式和符号
获取更多Notion 模板，可以查看这篇文章：