# Style.md — Vault 写作与 AI 写入规范

> 本文件是本 vault 的唯一风格权威。所有 AI（爬虫/总结/改写/新增）写入或修改笔记时必须遵守本文。冲突时以本文为准，覆盖模型默认习惯。
> 核心参考样本：`学习/Language/C C++`、`学习/ToolChain`、`学习/algorithm`、`学习/Bases/开发范式`、`Chores/JobInterviews` — 均为手写高密度样本。

## 1 写作风格与用语习惯

### 1.1 总则
- 高信息密度、精简全面。只讲知识本身，不堆过渡词、口头禅、拟人化比喻、寒暄总结（禁止"总的来说""不难看出""让我们一起"等废话）。
- 中文技术写作，术语中英混排时保持准确。首次出现的缩写给出全称，后续用缩写。
- 覆盖小众/边缘知识点。常用路径之外，冷门参数、异常分支、平台差异、历史原因一并记录，但不展开成独立长文。

### 1.2 句式与措辞
- 陈述句为主，主语省略或用无主句。慎用第一/第二人称。
- 一句一事实，避免"首先/其次/最后"串联。用小标题和列表代替过渡段落。
- 每个段落回答一个明确问题：是什么 / 为什么 / 怎么用 / 代价是什么。不符合的句子删除。
- 代码块必须提供"语言之外的新信息"。纯复述正文的示例代码禁止保留。
- 慎用 `**加粗**`，仅用于术语首次定义或关键结论，通篇不超过必要的强调。

### 1.3 术语与符号
- 数字、英文字母、行内代码与中文之间各加一个半角空格。例：`epoll 机制`、`1024 个 fd`、`调用 read`。
- 行内代码用反引号包裹，含 `<>` 的头文件必须写成 `` `<execinfo.h>` ``，否则 Typora 误解析为 HTML。
- 枚举值、常量、命令行、路径、API 名一律行内代码或代码块，不裸写中文引号内。

## 2 笔记撰写好恶与学习方式

### 2.1 喜欢
- 概念先行：每节开头 1-2 句说明该机制解决什么问题，再展开实现。
- 机制拆解：从动机出发，按数据流/调用链路完整追踪（调用方 → 内核/框架 → 返回路径），关键结构与关键函数点名到文件/符号。
- 对比表格：凡有 2 种以上方案，必用表格对比维度（原理/代价/适用场景/坑点），辅以最小可运行代码示例。
- 动手验证：给出可编译/可运行的最小复现，鼓励改代码加功能验证理解。
- 问题驱动：在 `ToolChain` 等实战笔记中以"真实报错 → 根因 → 修复 → 验证"结构沉淀，保留关键日志与最小复现配置。

### 2.2 不喜欢
- 罗列无关内容、堆砌背景故事、重复已掌握的常识。
- 为凑字数展开的赘述、重复的总结段、AI 套话（"综上所述""值得注意的是"连续出现）。
- 冗余代码块、不可运行的伪代码、与正文零增量的示例。

### 2.3 学习路径模板（单篇笔记内）
```md
# 主题
参考：[标题](链接)
## 解决什么问题 / 背景
## 核心机制 / 数据流
## 关键代码与数据结构
## 对比与选型
## 坑点 / 易忽略点 / 最佳实践
## 引申（≤2 层，见 2.4）
```
- `参考` 置于标题下方首行，多个链接各占一行。
- 跨笔记引用用 `[[笔记名#标题]]` 建立关联。

### 2.4 引申深度约束（硬约束）
- 引申不超过 2 层。达到 2 层即停止，不再展开第 3 层。
- 示例：学习 `struct` → 允许分 2 个分支，每分支再展开一层即止
    - 分支 1：控制结构体内存对齐的方法 → 位域/`__attribute__((packed))`/`std::align`/预编译指令等（到此为止）
    - 分支 2：结构体与类的区别 → C/C++ 中空类/空结构体最小大小规则（到此为止）
- 需要更深时，改为新开一篇笔记并用 `[[新笔记]]` 链接，不在当前篇继续深入。

### 2.5 组织与论证习惯
- 优先"定义 → 动机 → 实现 → 验证 → 对比 → 坑点"六段式，缺段可省但不乱序。
- 涉及性能/正确性结论时，给出可验证依据（源码位置、手册章节、实测数据），避免主观断言。

## 3 文件命名与编排结构

### 3.1 目录结构（现状即规范）
```
学习/
  Bases/开发范式/ /操作系统/ /数据结构/ /计算机组成原理/ /计算机网络/
  Language/C C++/ /DB/ /ScriptLangs/ ...
  ToolChain/
  algorithm/
  ProjectsStudy/ AI/ Fragments/
Chores/JobInterviews/
Files & LongText/Attachments/   # 全库附件统一目录（见 .obsidian/app.json）
Daily/
```
- 新增主题按学科归入 `学习/<一级>/<二级>`，不自创顶级目录。面试题归 `Chores/JobInterviews`。
- 单篇笔记聚焦单一主题，不做跨领域大杂烩。大主题拆多篇，用 `[[wikilink]]` 串联。

### 3.2 命名
- 文件名使用中文或中英混合，直白反映主题。例：`IO复用模型.md`、`C++开发范式和术语.md`、`CMake.md`。
- 允许空格（如 `C C++` 目录名、`C++ Runoob Tutoral.md`），但新增的文件/目录不允许空格，使用大驼峰命名规范
- 附件（截图/录屏）由 Obsidian 自动命名 `Pasted image ...png` / `recording N.gif`，统一落 `Files & LongText/Attachments`，正文用 `![[附件名]]` 引用，不使用相对路径手写 `../../../`。
- 禁止用 `01-`、`1.` 这类序号前缀做标题或文件名排序（标题中不加序号）。

### 3.3 单篇内部结构
- `#` 仅用于篇名，`##` 为一级章节，`###` 为子节，依次递进，不跳级。
- 标题文字简短概括正文，不在标题中使用序号、emoji、装饰符号。
- 章节内用嵌套列表表达层级与步骤，避免平铺多个同级列表。步骤类用有序列表，可选路径用无序列表嵌套。
- 表格与引用块前各保留一个空行以保证渲染，见第 4 节。

### 3.4 Frontmatter（可选）
- 教程/转载类笔记可在文件首部添加：
```md
---
created: YYYY年M月D日HH:MM:SS
source: https://...
---
```
- 日常手写笔记可不加，不强制补全。

## 4 Markdown 书写规范（来自 markdown-style skill）

### 4.1 紧凑排版
- 段落之间、段落与标题之间、标题与标题之间不留空行。
- 列表项各自独立一行，不为省行而合并。

### 4.2 列表与缩进
- 有层级/有顺序的内容用嵌套列表，不用 `①②` 等符号序号。
- 缩进使用 4 空格的 tab。

### 4.3 链接与引用
- 内部引用：`[[#标题内容]]` 或 `[[笔记名#标题]]`。
- 外部链接：`[链接标题](链接)`，多个各占一行。
- 图片/附件：`![[文件名.png]]`。

### 4.4 代码与表格
- 行内代码用 `` ` `` 包裹，参考 1.3。
- 表格之前必须有一个换行。

### 4.5 引用块（知识卡片）
```md
正文内容
> [!Note] 标题
> 正文内容
> - 列表项
```
- 可用标签：`Note` / `Info` / `Warning`。与上方正文之间保留一个换行。

## 5 笔记索引工具（notes-index）

> 工具位置：`~/notes-index/`（Python 模块，随 vault 独立于笔记库）。数据库默认在 `~/.cache/notes-index/index.db`（vault 外，不被 Obsidian 索引）。
> 用途：维护全库 md 的 ≤3 级标题索引与精简描述，供 AI 写入前查重、写入后校验。

### 5.1 命令

```bash
VAULT="/mnt/windows-e/file_storage/obsidian-vault"
# 全量扫描并更新索引（updateRecord）
python3 -m notes_index --vault "$VAULT" update
# 只对比不写库（dry-run）
python3 -m notes_index --vault "$VAULT" update --no-write
# 对比单个文件（isUpdate）；ALL 表示全库
python3 -m notes_index --vault "$VAULT" check <相对路径|ALL>
# 相似内容检索（lookup）
python3 -m notes_index --vault "$VAULT" lookup <关键词> [--limit 10]
# 写入标题/文件描述
python3 -m notes_index --vault "$VAULT" desc --file <相对路径> [--title <标题>] --desc <精简描述>
```

- 输出均为 JSON。`check` 返回 `added` / `renamed` / `deleted` / `need_desc` / `changed_files`；`lookup` 返回相似标题条目（含文件路径、标题、得分）。
- `desc` 的 `--title` 缺省时表示写文件描述；指定 `--title` 则写该标题的 desc。

### 5.2 与 AI 写入的协作

- 写入前：对当前要写的内容提炼 2-3 个关键词，逐个调用 `lookup`。结果非空则检查是否真相似；若真相似，在待写入内容上方单开一行用 `[[/相对路径/到/文件|description]]` 链接到对应标题，description 由 AI 编写（精简）。
- 写入后：调用 `check <当前文件>`，若返回的标题有更新（added/renamed/deleted），说明库内记录落后，需要调 `update` 同步。
- `update` 仅在用户要求时才调用，不自动触发。
- 新标题的 `desc` 缺失时 `check` 会出现在 `need_desc` 中，AI 可调 `desc` 补写，使后续 `lookup` 检索更准。

### 5.3 跨笔记引用语法

- 相似内容引用：`[[/相对路径/到/文件.md|description]]`（description 为 AI 撰写的精简说明）。
- 站内常规引用保持 `[[笔记名#标题]]` 不变；`[[/path/to/file|desc]]` 仅用于 lookup 命中后的相似内容关联。

## 6 AI 写入/改写执行清单

### 6.1 写入前
- 确认目标目录与文件名符合 3.1-3.2；不确定时复用最近同类笔记的目录。
- 爬取/总结类内容先去重：已在库内存在的知识点不重复建篇，改为在原篇追加小节并补充 `参考` 链接。
- 用 2-3 个关键词调 `lookup` 查库内相似内容；命中且确认真相似时按 5.2/5.3 加 `[[/路径|说明]]` 引用行，避免重复成篇。

### 6.2 写入时
- 遵守 1-4 节全部约束；输出中文，术语准确，零废话。
- 代码块注明语言（`cpp`/`cmake`/`bash`/`yaml` 等），保证可编译/可执行或明确标注"示意"。
- 涉及对比必用表格，涉及流程必给出数据流/调用链路。
- 引申严格执行 2 层上限，超限改为新笔记链接。

### 6.3 写入后自检（AI 必须逐条勾选）
- [ ] 无过渡废话、无拟人化口语、无重复总结段
- [ ] 中英/数字/代码与中文之间有空格，`<头文件>` 已用行内代码包裹
- [ ] 标题无序号、段落紧凑、列表未合并、表格/引用块前有换行、缩进为 4 空格
- [ ] 对比表格与代码块提供了正文之外的新信息，无冗余块
- [ ] 引申不超过 2 层，超限已拆新篇并用 `[[wikilink]]` 关联
- [ ] 附件已落 `Files & LongText/Attachments` 并用 `![[附件名]]` 引用
- [ ] 已更新 `参考` 链接，未删除用户已有内容（只追加/修正）
- [ ] 已调 `lookup` 查重，命中相似内容已加 `[[/路径|说明]]` 引用行
- [ ] 已调 `check` 校验目标文件标题，必要时已 `update` 同步索引库

### 6.4 禁止事项
- 禁止删除或重写用户已有段落（只追加新小节或在原小节内补充）；禁止为格式化而改动用户已固定的表述。
- 禁止为单篇写入过多引申分支（>2 分支需拆篇）。
- 禁止输出 emoji、寒暄、自我指代的冗余开场/收尾。
- 禁止在用户未要求时自动调用 `update`（索引同步仅限用户要求或写入自检后）。
